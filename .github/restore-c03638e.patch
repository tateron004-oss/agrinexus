diff --git a/public/app.js b/public/app.js
index acc1704..e3271b6 100644
--- a/public/app.js
+++ b/public/app.js
@@ -49988,6 +49988,8 @@ async function executeGenesisWorkspaceFromFinalTranscript(transcript = "") {
   if (lastGenesisTranscriptWorkspaceExecution.command === normalized && now - lastGenesisTranscriptWorkspaceExecution.at < 5000) return false;
   const action = genesisWorkspaceActionFromFinalTranscript(command);
   if (!action) return false;
+  nexusGenesisExperienceActivated = true;
+  nexusTrueExperienceSessionStarted = true;
   lastGenesisTranscriptWorkspaceExecution = { command: normalized, at: now };
   try {
     const result = { genesisAction: action, response: command, source: "openai-realtime-final-transcript" };
diff --git a/scripts/nexus-genesis-voice-workspace-bridge-qa.js b/scripts/nexus-genesis-voice-workspace-bridge-qa.js
index 5307080..7847119 100644
--- a/scripts/nexus-genesis-voice-workspace-bridge-qa.js
+++ b/scripts/nexus-genesis-voice-workspace-bridge-qa.js
@@ -2,3 +2,3 @@
 const server=fs.readFileSync(path.join(root,'server.js'),'utf8'); const app=fs.readFileSync(path.join(root,'public/app.js'),'utf8');
-const checks=[['authoritative action builder',server.includes('function nexusGenesisWorkspaceAction')],['native result carries action',server.includes('genesisAction')&&server.includes('redirectSection: genesisAction?.workspace || "agent"')],['map tool always creates action',server.includes('toolNames.includes("nexus_maps_route")')],['map navigation requires tool',server.includes('must call nexus_maps_route')],['verified workspace returned to model',app.includes('result.genesisAcknowledgement?.verified === true')&&app.includes('The visible ${workspaceName} workspace opened')],['map route entities',server.includes('originMatch')&&server.includes('destination')&&server.includes('countryMatch')],['workforce entities',server.includes('jobMatch')&&server.includes('locationMatch')&&server.includes('location: locationMatch')],['marketplace product entity',server.includes('cropMatch')&&server.includes('product: cropMatch')],['health intake entity',server.includes('intakeType:')],['one authoritative browser bridge',app.includes('async function runAuthoritativeGenesisWorkspaceBridge')&&app.includes('const genesisWorkspaceBridgeRequests = new Map()')],['raw Realtime path enters bridge',app.includes('callId: call.callId')&&app.includes('await runAuthoritativeGenesisWorkspaceBridge(result, {')],['SDK Realtime path enters bridge',app.includes('await runAuthoritativeGenesisWorkspaceBridge(result, { correlationId });')],['final transcript enters workspace executor',app.includes('conversation.item.input_audio_transcription.completed')&&app.includes('executeGenesisWorkspaceFromFinalTranscript(payload.acceptanceText)')],['final transcript execution is model independent',app.includes('function genesisWorkspaceActionFromFinalTranscript')&&app.includes('source: "openai-realtime-final-transcript"')],['final transcript covers required workspaces',app.includes('routeRequest ? "map" : workforceRequest ? "workforce" : marketplaceRequest ? "trade" : healthRequest ? "health" : "learning"')],['duplicate transcript execution guarded',app.includes('lastGenesisTranscriptWorkspaceExecution')&&app.includes('now - lastGenesisTranscriptWorkspaceExecution.at < 5000')],['registered workforce launcher',app.includes('workforce: "workforce"')&&!app.includes('workforce: "jobs"')],['browser dispatcher',app.includes('function dispatchGenesisWorkspaceActionVerified')],['browser acknowledgement',app.includes('genesis.workspace.acknowledged')&&app.includes('verified,')],['map destination population',app.includes('field.dataset.nexusRealtimeField = key')&&app.includes('destination: ["destinationAddress", "destination"]')],['non-map field population',app.includes('genesisWorkspacePrefill')&&app.includes('populatedValues')],['live microphone acknowledgement',app.includes('nexusPermanentMicrophoneStream?.getAudioTracks?.().some')],['function-window invocation',app.includes('openNexusFunctionWindow(functionId')&&app.includes('source: "openai-realtime"')],['matching request id',app.includes('const requestId = String(action.requestId || "")')]];
+const checks=[['authoritative action builder',server.includes('function nexusGenesisWorkspaceAction')],['native result carries action',server.includes('genesisAction')&&server.includes('redirectSection: genesisAction?.workspace || "agent"')],['map tool always creates action',server.includes('toolNames.includes("nexus_maps_route")')],['map navigation requires tool',server.includes('must call nexus_maps_route')],['verified workspace returned to model',app.includes('result.genesisAcknowledgement?.verified === true')&&app.includes('The visible ${workspaceName} workspace opened')],['map route entities',server.includes('originMatch')&&server.includes('destination')&&server.includes('countryMatch')],['workforce entities',server.includes('jobMatch')&&server.includes('locationMatch')&&server.includes('location: locationMatch')],['marketplace product entity',server.includes('cropMatch')&&server.includes('product: cropMatch')],['health intake entity',server.includes('intakeType:')],['one authoritative browser bridge',app.includes('async function runAuthoritativeGenesisWorkspaceBridge')&&app.includes('const genesisWorkspaceBridgeRequests = new Map()')],['raw Realtime path enters bridge',app.includes('callId: call.callId')&&app.includes('await runAuthoritativeGenesisWorkspaceBridge(result, {')],['SDK Realtime path enters bridge',app.includes('await runAuthoritativeGenesisWorkspaceBridge(result, { correlationId });')],['final transcript enters workspace executor',app.includes('conversation.item.input_audio_transcription.completed')&&app.includes('executeGenesisWorkspaceFromFinalTranscript(payload.acceptanceText)')],['final transcript execution is model independent',app.includes('function genesisWorkspaceActionFromFinalTranscript')&&app.includes('source: "openai-realtime-final-transcript"')],['final transcript activates Genesis mission renderer',app.includes('nexusGenesisExperienceActivated = true;')&&app.includes('nexusTrueExperienceSessionStarted = true;')],['final transcript covers required workspaces',app.includes('routeRequest ? "map" : workforceRequest ? "workforce" : marketplaceRequest ? "trade" : healthRequest ? "health" : "learning"')],['duplicate transcript execution guarded',app.includes('lastGenesisTranscriptWorkspaceExecution')&&app.includes('now - lastGenesisTranscriptWorkspaceExecution.at < 5000')],['registered workforce launcher',app.includes('workforce: "workforce"')&&!app.includes('workforce: "jobs"')],['browser dispatcher',app.includes('function dispatchGenesisWorkspaceActionVerified')],['browser acknowledgement',app.includes('genesis.workspace.acknowledged')&&app.includes('verified,')],['map destination population',app.includes('field.dataset.nexusRealtimeField = key')&&app.includes('destination: ["destinationAddress", "destination"]')],['non-map field population',app.includes('genesisWorkspacePrefill')&&app.includes('populatedValues')],['live microphone acknowledgement',app.includes('nexusPermanentMicrophoneStream?.getAudioTracks?.().some')],['function-window invocation',app.includes('openNexusFunctionWindow(functionId')&&app.includes('source: "openai-realtime"')],['matching request id',app.includes('const requestId = String(action.requestId || "")')]];
