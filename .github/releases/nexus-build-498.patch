diff --git a/public/app.js b/public/app.js
index 4bcd1b71..673c131f 100644
--- a/public/app.js
+++ b/public/app.js
@@ -1343,8 +1343,8 @@ const nexusProductIdentity = Object.freeze({
 });
 const assistantFullName = "AgriNexus";
 const assistantShortName = "Nexus";
-const AGRINEXUS_BUILD_VERSION = "nexus-behavior-497";
-const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442";
+const AGRINEXUS_BUILD_VERSION = "nexus-behavior-498";
+const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443";
 const VOICE_RESTART_DELAY_MS = 320;
 const VOICE_UI_FOCUS_DELAY_MS = 80;
 const VOICE_ATTENTION_DELAY_MS = 900;
diff --git a/public/index.html b/public/index.html
index ffe3dcf9..bb6706ac 100644
--- a/public/index.html
+++ b/public/index.html
@@ -9,11 +9,11 @@
   <meta name="mobile-web-app-capable" content="yes">
   <meta name="apple-mobile-web-app-title" content="Nexus Genesis | AgriNexus">
   <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
-  <link rel="manifest" href="/manifest.webmanifest?v=nexus-behavior-497">
+  <link rel="manifest" href="/manifest.webmanifest?v=nexus-behavior-498">
   <link rel="icon" href="/icons/agri-nexus-192.png" type="image/png">
   <link rel="apple-touch-icon" href="/icons/agri-nexus-192.png">
   <link rel="stylesheet" href="/vendor/leaflet/leaflet.css?v=1.9.4">
-  <link rel="stylesheet" href="/styles.css?v=nexus-behavior-497">
+  <link rel="stylesheet" href="/styles.css?v=nexus-behavior-498">
   <script type="importmap">
     {
       "imports": {
@@ -2227,9 +2227,9 @@
   <script src="/nexus-genesis-conversational-mode-orchestrator.js?v=nexus-genesis-conversational-mode-orchestrator-2"></script>
   <script src="/nexus-os-agrinexus-deployment-profile.js?v=nexus-os-agrinexus-deployment-1"></script>
   <script src="/nexus-os-health-workforce-safety-pack.js?v=nexus-os-health-workforce-safety-1"></script>
-  <script src="/nexus-genesis-voice-runtime-manager.js?v=nexus-behavior-497"></script>
+  <script src="/nexus-genesis-voice-runtime-manager.js?v=nexus-behavior-498"></script>
   <script src="/browser-action-controller.js?v=nexus-action-controller-1"></script>
-  <script src="/app.js?v=nexus-behavior-497"></script>
+  <script src="/app.js?v=nexus-behavior-498"></script>
   <script src="/nexus-voice-demo-shell.js?v=nexus-behavior-305"></script>
 </body>
 </html>
diff --git a/public/sw.js b/public/sw.js
index 54d559f7..7e211a73 100644
--- a/public/sw.js
+++ b/public/sw.js
@@ -1,5 +1,5 @@
-const CACHE_NAME = "agrinexus-pwa-v442";
-const BUILD_VERSION = "nexus-behavior-497";
+const CACHE_NAME = "agrinexus-pwa-v443";
+const BUILD_VERSION = "nexus-behavior-498";
 const APP_SHELL = [
   "/",
   "/index.html",
diff --git a/scripts/nexus-companion-voice-activation-flow-qa.js b/scripts/nexus-companion-voice-activation-flow-qa.js
index 5fe50632..bf5da70b 100644
--- a/scripts/nexus-companion-voice-activation-flow-qa.js
+++ b/scripts/nexus-companion-voice-activation-flow-qa.js
@@ -31,7 +31,7 @@ assert(!app.includes("Nexus is blocked from executing externally."), "raw blocke
 assert(app.includes("I can help here, but live external actions need a connected service."), "friendly external-action limitation must be present");
 assert(index.includes('<meta name="mobile-web-app-capable" content="yes">'), "index should include modern mobile-web-app-capable metadata to avoid browser warning");
 assert(!index.includes('name="apple-mobile-web-app-capable"'), "index should avoid deprecated apple-mobile-web-app-capable warning");
-assert(index.includes("/manifest.webmanifest?v=nexus-behavior-497"), "index should version the manifest link to avoid stale manifest warnings");
+assert(index.includes("/manifest.webmanifest?v=nexus-behavior-498"), "index should version the manifest link to avoid stale manifest warnings");
 assert(manifest.includes('"enctype": "application/x-www-form-urlencoded"'), "manifest share target should specify enctype to avoid browser warning");
 
 includesAll(app, [
diff --git a/scripts/nexus-conversation-workflow-transition-engine-qa.js b/scripts/nexus-conversation-workflow-transition-engine-qa.js
index a19c9cd1..5cfcf44a 100644
--- a/scripts/nexus-conversation-workflow-transition-engine-qa.js
+++ b/scripts/nexus-conversation-workflow-transition-engine-qa.js
@@ -208,7 +208,7 @@ assert(reminderProposal.options.some(workflow => workflow.workflowId === "daily-
 assert(/no scheduled reminder receipt/i.test(reminderProposal.options.find(workflow => workflow.workflowId === "daily-life.reminder-proposal").receiptBehavior), "reminder proposal does not fake scheduling");
 
 includes(index, "/nexus-conversation-workflow-transition-engine.js", "index script loading");
-includes(index, "/app.js?v=nexus-behavior-497", "app cache version bump");
+includes(index, "/app.js?v=nexus-behavior-498", "app cache version bump");
 includes(app, "nexusConversationWorkflowTransitionState", "app state");
 includes(app, "function handleNexusConversationWorkflowTransitionCommand", "app handler");
 includes(app, "renderNexusConversationWorkflowTransitionCard()", "same-conversation renderer");
diff --git a/scripts/nexus-endgame-production-platform-qa.js b/scripts/nexus-endgame-production-platform-qa.js
index 11ef1201..d1661ed0 100644
--- a/scripts/nexus-endgame-production-platform-qa.js
+++ b/scripts/nexus-endgame-production-platform-qa.js
@@ -226,9 +226,9 @@ check("multilingual status avoids full certification claim", hasAll(server, [
   "noFullCertificationClaim"
 ]));
 
-check("service worker/cache bumped", server.includes("nexus-behavior-497")
-  && app.includes("nexus-behavior-497")
-  && sw.includes("agrinexus-pwa-v442"));
+check("service worker/cache bumped", server.includes("nexus-behavior-498")
+  && app.includes("nexus-behavior-498")
+  && sw.includes("agrinexus-pwa-v443"));
 
 check("package alias exists", pkg.scripts["qa:nexus-endgame-production-platform"] === "node scripts/nexus-endgame-production-platform-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-endgame-production-platform-qa.js"));
diff --git a/scripts/nexus-experience-polish-layer-qa.js b/scripts/nexus-experience-polish-layer-qa.js
index 42e091c6..e1def923 100644
--- a/scripts/nexus-experience-polish-layer-qa.js
+++ b/scripts/nexus-experience-polish-layer-qa.js
@@ -169,7 +169,7 @@ check("safe suites include experience polish QA", hasAll(qaSuite, [
 ]));
 
 check("build token bumped with frontend change", hasAll(app, [
-  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"'
+  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"'
 ]));
 
 if (failures) {
diff --git a/scripts/nexus-genesis-browser-voice-acceptance-qa.js b/scripts/nexus-genesis-browser-voice-acceptance-qa.js
index b3a38ef3..ac2dd6ac 100644
--- a/scripts/nexus-genesis-browser-voice-acceptance-qa.js
+++ b/scripts/nexus-genesis-browser-voice-acceptance-qa.js
@@ -131,7 +131,7 @@ assert(
   qaSuite.includes('"scripts/nexus-genesis-browser-voice-acceptance-qa.js"'),
   "voice/all-safe suite must include browser voice acceptance QA"
 );
-assert(index.includes("/app.js?v=nexus-behavior-497"), "index must bump app.js version so browser voice fixes load");
+assert(index.includes("/app.js?v=nexus-behavior-498"), "index must bump app.js version so browser voice fixes load");
 
 includesAll(acceptanceDoc, [
   "Nexus Genesis Real Browser Voice and Companion Acceptance",
diff --git a/scripts/nexus-genesis-live-provider-browser-smoke.js b/scripts/nexus-genesis-live-provider-browser-smoke.js
index b4de41ca..1011521f 100644
--- a/scripts/nexus-genesis-live-provider-browser-smoke.js
+++ b/scripts/nexus-genesis-live-provider-browser-smoke.js
@@ -25,6 +25,7 @@ const spokenJourneys = [
   { workspace: "live-knowledge", words: ["climate-smart", "sources"], command: "Nexus, use the internet to research current climate-smart agriculture information and show sources." },
   { workspace: null, words: [], provider: "google-cloud-translation", command: "Nexus, change language to Swahili and tell me good morning farmer." }
   ,{ workspace: null, words: [], cloudinaryProvider: "cloudinary", command: "Nexus, upload and verify the Cloudinary certification image." }
+  ,{ workspace: null, words: [], twilioProvider: "twilio", command: "Nexus, send a production test SMS to my owner test recipient. I explicitly confirm this test send." }
 ];
 const browserCandidates = process.platform === "win32"
   ? ["C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe", "C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe"]
@@ -250,6 +251,7 @@ async function main() {
         window.__NEXUS_WORKSPACE_ACKS__ = [];
         window.__NEXUS_TRANSLATION_TOOL_RESULTS__ = [];
         window.__NEXUS_CLOUDINARY_TOOL_RESULTS__ = [];
+        window.__NEXUS_TWILIO_TOOL_RESULTS__ = [];
         const nexusOriginalFetch = window.fetch.bind(window);
         window.fetch = async (...args) => {
           const response = await nexusOriginalFetch(...args);
@@ -267,6 +269,14 @@ async function main() {
                 publicId: String(payload?.cloudinaryReceipt?.publicId || ''),
                 response: String(payload.response || '')
               });
+              if (payload?.provider === 'twilio' && payload?.providerAction === 'sms.send') window.__NEXUS_TWILIO_TOOL_RESULTS__.push({
+                provider: String(payload.provider),
+                action: String(payload.providerAction),
+                succeeded: payload.providerSucceeded === true,
+                verified: payload.executionVerified === true,
+                sid: String(payload?.providerData?.sid || ''),
+                response: String(payload.response || '')
+              });
             }).catch(() => {});
           }
           return response;
@@ -383,6 +393,8 @@ async function main() {
       const translationSatisfied = translationResults.some(item => item.provider === 'google-cloud-translation' && item.response.trim());
       const cloudinaryResults = window.__NEXUS_CLOUDINARY_TOOL_RESULTS__ || [];
       const cloudinarySatisfied = cloudinaryResults.some(item => item.provider === 'cloudinary' && item.verified && item.secureDelivery && item.publicId && item.response.trim());
+      const twilioResults = window.__NEXUS_TWILIO_TOOL_RESULTS__ || [];
+      const twilioSatisfied = twilioResults.some(item => item.provider === 'twilio' && item.action === 'sms.send' && item.succeeded && item.verified && /^SM[A-Za-z0-9]+$/.test(item.sid) && item.response.trim());
       const lifecycleInterruptionCount = lifecycleEvents.filter(event => /interrupt|cancel-requested/.test(String(event.eventName || ''))).length;
       const interruptionSatisfied = interruptionCount + lifecycleInterruptionCount >= ${requiredInterruptions};
       window.__NEXUS_ACCEPTANCE_SNAPSHOT__ = {
@@ -400,10 +412,12 @@ async function main() {
         translationResults,
         cloudinarySatisfied,
         cloudinaryResults,
+        twilioSatisfied,
+        twilioResults,
         workspaceAckCount: workspaceAcks.length,
         workspaceResults
       };
-      if (!speechStarted || !responseDone || !modelAudio || !interruptionSatisfied || !workspacesSatisfied || !translationSatisfied || !cloudinarySatisfied) return null;
+      if (!speechStarted || !responseDone || !modelAudio || !interruptionSatisfied || !workspacesSatisfied || !translationSatisfied || !cloudinarySatisfied || !twilioSatisfied) return null;
       return {
         speechStarted,
         responseDone,
@@ -423,6 +437,7 @@ async function main() {
         workspaceResults,
         translationResults
         ,cloudinaryResults
+        ,twilioResults
       };
     })()`), Math.max(180000, expectedTurns * 30000), "synthetic spoken turn and model response");
 
diff --git a/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js b/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
index 04a32755..72b63a22 100644
--- a/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
+++ b/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
@@ -21,16 +21,16 @@ function notIncludes(source, needle, label) {
   assert(!source.includes(needle), `${label} must not include: ${needle}`);
 }
 
-["nexus-behavior-497", "agrinexus-pwa-v442"].forEach(marker => {
+["nexus-behavior-498", "agrinexus-pwa-v443"].forEach(marker => {
   includes(app, marker, `app marker ${marker}`);
   includes(server, marker, `server marker ${marker}`);
   includes(sw, marker, `service worker marker ${marker}`);
 });
-includes(index, "nexus-behavior-497", "index behavior marker nexus-behavior-497");
+includes(index, "nexus-behavior-498", "index behavior marker nexus-behavior-498");
 
 includes(index, 'id="nexusPermanentMicrophoneBtn"', "static microphone button");
 includes(index, "Enable microphone", "static microphone initial label");
-assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-497"), "microphone button must appear before app.js");
+assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-498"), "microphone button must appear before app.js");
 assert(!/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index), "static microphone button must not be hidden or disabled");
 
 [
diff --git a/scripts/nexus-genesis-realtime-production-migration-qa.js b/scripts/nexus-genesis-realtime-production-migration-qa.js
index 4ae34576..e87b0136 100644
--- a/scripts/nexus-genesis-realtime-production-migration-qa.js
+++ b/scripts/nexus-genesis-realtime-production-migration-qa.js
@@ -168,8 +168,8 @@ const scenarios = [
 ];
 
 const requirements = [
-  ["Build markers bumped", server.includes("nexus-behavior-497") && app.includes("nexus-behavior-497") && sw.includes("nexus-behavior-497") && index.includes("nexus-behavior-497")],
-  ["PWA markers bumped", server.includes("agrinexus-pwa-v442") && app.includes("agrinexus-pwa-v442") && sw.includes("agrinexus-pwa-v442")],
+  ["Build markers bumped", server.includes("nexus-behavior-498") && app.includes("nexus-behavior-498") && sw.includes("nexus-behavior-498") && index.includes("nexus-behavior-498")],
+  ["PWA markers bumped", server.includes("agrinexus-pwa-v443") && app.includes("agrinexus-pwa-v443") && sw.includes("agrinexus-pwa-v443")],
   ["Genesis runtime marker bumped", app.includes("nexus-genesis-voice-runtime-v456")],
   ["Realtime runtime version exists", server.includes("NEXUS_GENESIS_REALTIME_RUNTIME_VERSION") && app.includes("NEXUS_GENESIS_REALTIME_RUNTIME_VERSION")],
   ["Server runtime values", server.includes('const NEXUS_GENESIS_VOICE_RUNTIME_VALUES = new Set(["realtime", "disabled"])')],
diff --git a/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js b/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
index 86159e50..aa9cbaa6 100644
--- a/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
+++ b/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
@@ -66,7 +66,7 @@ includes(server, "create_response: true", "server VAD repeated response creation
 includes(server, "interrupt_response: true", "server VAD interruption support");
 includes(server, "silence_duration_ms: Number(env.OPENAI_REALTIME_SILENCE_DURATION_MS || 700)", "server VAD silence duration retained");
 
-["nexus-behavior-497", "agrinexus-pwa-v442"].forEach(marker => {
+["nexus-behavior-498", "agrinexus-pwa-v443"].forEach(marker => {
   includes(app, marker, `app marker ${marker}`);
   includes(server, marker, `server marker ${marker}`);
   includes(sw, marker, `service worker marker ${marker}`);
diff --git a/scripts/nexus-genesis-truthful-voice-state-qa.js b/scripts/nexus-genesis-truthful-voice-state-qa.js
index 840ef4f9..bd3c00c0 100644
--- a/scripts/nexus-genesis-truthful-voice-state-qa.js
+++ b/scripts/nexus-genesis-truthful-voice-state-qa.js
@@ -42,22 +42,22 @@ const speechSynthesis = sectionBetween(app, "function runNexusSpeechSynthesisCon
 const speechResume = sectionBetween(app, "function resumeVoiceListeningAfterSpeech", "function stopVoicePlayback", "speech restart");
 
 includesAll(app, [
-  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"',
-  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"',
+  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"',
+  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"',
   'NEXUS_GENESIS_VOICE_RUNTIME_VERSION = "nexus-genesis-voice-runtime-v456"'
 ], "app build");
 includesAll(index, [
-  "/manifest.webmanifest?v=nexus-behavior-497",
-  "/styles.css?v=nexus-behavior-497",
-  "/app.js?v=nexus-behavior-497"
+  "/manifest.webmanifest?v=nexus-behavior-498",
+  "/styles.css?v=nexus-behavior-498",
+  "/app.js?v=nexus-behavior-498"
 ], "index build");
 includesAll(server, [
-  'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497"',
-  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"'
+  'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498"',
+  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"'
 ], "server build");
 includesAll(sw, [
-  'CACHE_NAME = "agrinexus-pwa-v442"',
-  'BUILD_VERSION = "nexus-behavior-497"'
+  'CACHE_NAME = "agrinexus-pwa-v443"',
+  'BUILD_VERSION = "nexus-behavior-498"'
 ], "service worker build");
 
 includesAll(truthfulState, [
diff --git a/scripts/nexus-genesis-visible-product-acceptance-qa.js b/scripts/nexus-genesis-visible-product-acceptance-qa.js
index 27ca6966..86f3e36d 100644
--- a/scripts/nexus-genesis-visible-product-acceptance-qa.js
+++ b/scripts/nexus-genesis-visible-product-acceptance-qa.js
@@ -59,15 +59,15 @@ includes(server, 'edition: "genesis"', "server edition");
 assert.strictEqual(registry.productName, "Nexus Genesis | AgriNexus", "tool registry product identity");
 assert.strictEqual(registry.edition, "genesis", "tool registry edition");
 
-includes(index, "/manifest.webmanifest?v=nexus-behavior-497", "manifest cache marker");
-includes(index, "/styles.css?v=nexus-behavior-497", "stylesheet cache marker");
-includes(index, "/app.js?v=nexus-behavior-497", "app cache marker");
-includes(app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"', "app build marker");
-includes(app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"', "app PWA cache marker");
-includes(server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497"', "server build marker");
-includes(server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"', "server PWA cache marker");
-includes(sw, 'CACHE_NAME = "agrinexus-pwa-v442"', "service worker cache marker");
-includes(sw, 'BUILD_VERSION = "nexus-behavior-497"', "service worker build marker");
+includes(index, "/manifest.webmanifest?v=nexus-behavior-498", "manifest cache marker");
+includes(index, "/styles.css?v=nexus-behavior-498", "stylesheet cache marker");
+includes(index, "/app.js?v=nexus-behavior-498", "app cache marker");
+includes(app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"', "app build marker");
+includes(app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"', "app PWA cache marker");
+includes(server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498"', "server build marker");
+includes(server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"', "server PWA cache marker");
+includes(sw, 'CACHE_NAME = "agrinexus-pwa-v443"', "service worker cache marker");
+includes(sw, 'BUILD_VERSION = "nexus-behavior-498"', "service worker build marker");
 includes(server, 'if (!user && url.pathname !== "/api/config" && !boundedGenesisVoiceGuestRoutes.has(url.pathname))', "public redacted config route and bounded voice routes must not trigger guest console 401");
 
 excludes(index, "data-nexus-genesis-orb-entry", "index orb entry");
diff --git a/scripts/nexus-genesis-voice-e2e-software-qa.js b/scripts/nexus-genesis-voice-e2e-software-qa.js
index 58841809..e16c1f4e 100644
--- a/scripts/nexus-genesis-voice-e2e-software-qa.js
+++ b/scripts/nexus-genesis-voice-e2e-software-qa.js
@@ -35,8 +35,8 @@ function countMatches(source, regex) {
   return Array.from(source.matchAll(regex)).length;
 }
 
-const build = "nexus-behavior-497";
-const cache = "agrinexus-pwa-v442";
+const build = "nexus-behavior-498";
+const cache = "agrinexus-pwa-v443";
 
 [
   ["app", app],
@@ -50,7 +50,7 @@ includes(index, build, "index build marker");
 
 assert.equal(countMatches(index, /id="nexusPermanentMicrophoneBtn"/g), 1, "static HTML must contain exactly one permanent microphone button");
 assert.equal(countMatches(index, /id="nexusPermanentMicrophoneDock"/g), 1, "static HTML must contain exactly one permanent microphone dock");
-assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-497"), "microphone button must exist before app.js executes");
+assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-498"), "microphone button must exist before app.js executes");
 assert(!/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index), "microphone button must not be hidden or disabled in HTML");
 includes(index, "Enable microphone", "initial microphone label");
 includes(index, 'aria-describedby="nexusPermanentMicrophoneStatus"', "accessible microphone status binding");
diff --git a/scripts/nexus-genesis-voice-native-front-door-qa.js b/scripts/nexus-genesis-voice-native-front-door-qa.js
index 05f53b70..252b1ef7 100644
--- a/scripts/nexus-genesis-voice-native-front-door-qa.js
+++ b/scripts/nexus-genesis-voice-native-front-door-qa.js
@@ -141,10 +141,10 @@ includesAll(speechResume, [
   "recognition-restart-requested"
 ], "speech restart");
 
-includesAll(index, ["/app.js?v=nexus-behavior-497", "/styles.css?v=nexus-behavior-497"], "index cache");
-includesAll(app, ["nexus-behavior-497", "agrinexus-pwa-v442", "nexus-genesis-voice-runtime-v456"], "app cache");
-includesAll(server, ["nexus-behavior-497", "agrinexus-pwa-v442"], "server cache");
-includesAll(sw, ["nexus-behavior-497", "agrinexus-pwa-v442"], "service worker cache");
+includesAll(index, ["/app.js?v=nexus-behavior-498", "/styles.css?v=nexus-behavior-498"], "index cache");
+includesAll(app, ["nexus-behavior-498", "agrinexus-pwa-v443", "nexus-genesis-voice-runtime-v456"], "app cache");
+includesAll(server, ["nexus-behavior-498", "agrinexus-pwa-v443"], "server cache");
+includesAll(sw, ["nexus-behavior-498", "agrinexus-pwa-v443"], "service worker cache");
 
 assert(
   packageJson.scripts["qa:nexus-genesis-voice-native-front-door"] === "node scripts/nexus-genesis-voice-native-front-door-qa.js",
diff --git a/scripts/nexus-internet-resource-assistant-platform-qa.js b/scripts/nexus-internet-resource-assistant-platform-qa.js
index 3f9b537e..28324bf9 100644
--- a/scripts/nexus-internet-resource-assistant-platform-qa.js
+++ b/scripts/nexus-internet-resource-assistant-platform-qa.js
@@ -224,9 +224,9 @@ check("Standard User first screen remains conversation-first and uncluttered", h
   'data-standard-user-startup-visible="false" hidden aria-hidden="true"'
 ]) && modeLabels.every(label => app.includes(label)) && !app.includes("workflow tab wall"));
 
-check("service worker/cache bumped for frontend change", hasAll(server, ["nexus-behavior-497", "agrinexus-pwa-v442"])
-  && hasAll(app, ["nexus-behavior-497", "agrinexus-pwa-v442"])
-  && hasAll(sw, ["nexus-behavior-497", "agrinexus-pwa-v442"]));
+check("service worker/cache bumped for frontend change", hasAll(server, ["nexus-behavior-498", "agrinexus-pwa-v443"])
+  && hasAll(app, ["nexus-behavior-498", "agrinexus-pwa-v443"])
+  && hasAll(sw, ["nexus-behavior-498", "agrinexus-pwa-v443"]));
 
 check("package alias exists", pkg.scripts["qa:nexus-internet-resource-assistant-platform"] === "node scripts/nexus-internet-resource-assistant-platform-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-internet-resource-assistant-platform-qa.js"));
diff --git a/scripts/nexus-internet-resource-provider-pathway-qa.js b/scripts/nexus-internet-resource-provider-pathway-qa.js
index 33238366..1b249146 100644
--- a/scripts/nexus-internet-resource-provider-pathway-qa.js
+++ b/scripts/nexus-internet-resource-provider-pathway-qa.js
@@ -143,7 +143,7 @@ check("standard user first screen remains icon-first", [
 
 check("false live claims remain blocked", !/\b(Nexus|we|I)\s+(sent|called|processed|dispatched|fulfilled|connected you to a live provider|completed payment|placed a call)\b/i.test(app + server));
 
-check("service worker/cache bumped", server.includes("nexus-behavior-497") && app.includes("nexus-behavior-497") && sw.includes("agrinexus-pwa-v442"));
+check("service worker/cache bumped", server.includes("nexus-behavior-498") && app.includes("nexus-behavior-498") && sw.includes("agrinexus-pwa-v443"));
 check("package alias exists", pkg.scripts["qa:nexus-internet-resource-provider-pathway"] === "node scripts/nexus-internet-resource-provider-pathway-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-internet-resource-provider-pathway-qa.js"));
 
diff --git a/scripts/nexus-layered-internet-intelligence-qa.js b/scripts/nexus-layered-internet-intelligence-qa.js
index f85f0f99..1898508e 100644
--- a/scripts/nexus-layered-internet-intelligence-qa.js
+++ b/scripts/nexus-layered-internet-intelligence-qa.js
@@ -161,15 +161,15 @@ includes(app, "No citations are shown because live retrieval is not configured",
 ].forEach(token => includes(css, token, `layered css ${token}`));
 
 [
-  "nexus-behavior-497",
-  "agrinexus-pwa-v442"
+  "nexus-behavior-498",
+  "agrinexus-pwa-v443"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
   includes(sw, token, `service worker build ${token}`);
 });
-includes(index, "/styles.css?v=nexus-behavior-497", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-497", "app cache bust");
+includes(index, "/styles.css?v=nexus-behavior-498", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-498", "app cache bust");
 
 assert.strictEqual(
   packageJson.scripts["qa:nexus-layered-internet-intelligence"],
diff --git a/scripts/nexus-live-knowledge-retrieval-qa.js b/scripts/nexus-live-knowledge-retrieval-qa.js
index 2ef9a688..82824a41 100644
--- a/scripts/nexus-live-knowledge-retrieval-qa.js
+++ b/scripts/nexus-live-knowledge-retrieval-qa.js
@@ -207,15 +207,15 @@ const setupDoc = read("docs/NEXUS_LIVE_KNOWLEDGE_PROVIDER_CONFIGURATION.md");
 ].forEach(token => excludes(setupDoc, token, `setup documentation secret pattern ${token}`));
 
 [
-  "nexus-behavior-497",
-  "agrinexus-pwa-v442"
+  "nexus-behavior-498",
+  "agrinexus-pwa-v443"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
   includes(sw, token, `service worker build ${token}`);
 });
-includes(index, "/styles.css?v=nexus-behavior-497", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-497", "app cache bust");
+includes(index, "/styles.css?v=nexus-behavior-498", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-498", "app cache bust");
 
 assert.strictEqual(
   packageJson.scripts["qa:nexus-live-knowledge-retrieval"],
diff --git a/scripts/nexus-panel-tab-click-repair-qa.js b/scripts/nexus-panel-tab-click-repair-qa.js
index ddaaa216..0dc9f5de 100644
--- a/scripts/nexus-panel-tab-click-repair-qa.js
+++ b/scripts/nexus-panel-tab-click-repair-qa.js
@@ -137,16 +137,16 @@ formButtonMatches.forEach(buttonHtml => {
 });
 
 [
-  ["nexus-behavior-497", "app build version", "server build version"],
-  ["agrinexus-pwa-v442", "app cache version", "server cache version"]
+  ["nexus-behavior-498", "app build version", "server build version"],
+  ["agrinexus-pwa-v443", "app cache version", "server cache version"]
 ].forEach(([version, appLabel, serverLabel]) => {
   includes(app, version, `${appLabel} ${version}`);
   includes(server, version, `${serverLabel} ${version}`);
 });
-includes(sw, 'CACHE_NAME = "agrinexus-pwa-v442"', "service worker cache bump");
-includes(sw, 'BUILD_VERSION = "nexus-behavior-497"', "service worker build bump");
-includes(index, "/styles.css?v=nexus-behavior-497", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-497", "app cache bust");
+includes(sw, 'CACHE_NAME = "agrinexus-pwa-v443"', "service worker cache bump");
+includes(sw, 'BUILD_VERSION = "nexus-behavior-498"', "service worker build bump");
+includes(index, "/styles.css?v=nexus-behavior-498", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-498", "app cache bust");
 
 [
   "live emergency response enabled",
diff --git a/scripts/nexus-platform-predictive-intelligence-qa.js b/scripts/nexus-platform-predictive-intelligence-qa.js
index c8d6cff0..08eb3653 100644
--- a/scripts/nexus-platform-predictive-intelligence-qa.js
+++ b/scripts/nexus-platform-predictive-intelligence-qa.js
@@ -204,8 +204,8 @@ domains.forEach(domain => {
   "WhatsApp sent successfully"
 ].forEach(token => excludes(`${app}\n${server}`, token, `unsafe predictive execution claim ${token}`));
 
-includes(serviceWorker, "agrinexus-pwa-v442", "service worker cache bump");
-includes(serviceWorker, "nexus-behavior-497", "service worker build version bump");
+includes(serviceWorker, "agrinexus-pwa-v443", "service worker cache bump");
+includes(serviceWorker, "nexus-behavior-498", "service worker build version bump");
 assert.strictEqual(packageJson.scripts["qa:nexus-platform-predictive-intelligence"], "node scripts/nexus-platform-predictive-intelligence-qa.js", "platform package alias should run platform QA");
 includes(qaSuite, "scripts/nexus-platform-predictive-intelligence-qa.js", "qa-suite should include platform predictive intelligence QA");
 
diff --git a/scripts/nexus-production-audio-pipeline-qa.js b/scripts/nexus-production-audio-pipeline-qa.js
index f3a12e80..ab1b4560 100644
--- a/scripts/nexus-production-audio-pipeline-qa.js
+++ b/scripts/nexus-production-audio-pipeline-qa.js
@@ -74,10 +74,10 @@ assert(app.includes("I will not claim I can hear audio until recognition starts.
 assert(app.includes("Microphone permission may be available, but recognition is not actively listening now."), "permission-granted recognition failure must be truthful");
 assert(app.includes("Genesis keeps the home screen audio-first"), "voice fallback copy must keep Genesis home audio-first");
 
-assert(app.includes("nexus-behavior-497"), "app build identifier must be bumped");
-assert(server.includes("nexus-behavior-497"), "server build identifier must be bumped");
-assert(app.includes("agrinexus-pwa-v442"), "app PWA cache identifier must be bumped");
-assert(server.includes("agrinexus-pwa-v442"), "server PWA cache identifier must be bumped");
+assert(app.includes("nexus-behavior-498"), "app build identifier must be bumped");
+assert(server.includes("nexus-behavior-498"), "server build identifier must be bumped");
+assert(app.includes("agrinexus-pwa-v443"), "app PWA cache identifier must be bumped");
+assert(server.includes("agrinexus-pwa-v443"), "server PWA cache identifier must be bumped");
 const autoStartBlock = app.slice(app.indexOf("async function maybeStartGenesisRecognitionAfterGrantedPermission"), app.indexOf("function nexusVoiceAudioDebugEnabled"));
 assert(autoStartBlock.includes("nexusMicrophonePermissionCanAttemptStart(permission)"), "prompt/granted/browser-managed permission states must be eligible for getUserMedia attempt");
 assert(!autoStartBlock.includes("granted-or-browser-managed"), "auto-start must not compare against legacy display labels");
diff --git a/scripts/nexus-production-platform-rails-qa.js b/scripts/nexus-production-platform-rails-qa.js
index 19968875..cb98c2b4 100644
--- a/scripts/nexus-production-platform-rails-qa.js
+++ b/scripts/nexus-production-platform-rails-qa.js
@@ -137,8 +137,8 @@ function excludes(source, token, label) {
 ].forEach(token => includes(css, token, `production rail CSS ${token}`));
 
 [
-  "nexus-behavior-497",
-  "agrinexus-pwa-v442"
+  "nexus-behavior-498",
+  "agrinexus-pwa-v443"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
diff --git a/scripts/nexus-realtime-microphone-visibility-qa.js b/scripts/nexus-realtime-microphone-visibility-qa.js
index c759aa29..a87aa57f 100644
--- a/scripts/nexus-realtime-microphone-visibility-qa.js
+++ b/scripts/nexus-realtime-microphone-visibility-qa.js
@@ -39,18 +39,18 @@ const realtimeStartup = between(app, "async function startOpenAiAgentsRealtimeVo
 const checks = [
   [
     "Build and cache advanced",
-    app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"') &&
-      app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"') &&
-      server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497"') &&
-      sw.includes('const CACHE_NAME = "agrinexus-pwa-v442"') &&
-      index.includes("/app.js?v=nexus-behavior-497")
+    app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"') &&
+      app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"') &&
+      server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498"') &&
+      sw.includes('const CACHE_NAME = "agrinexus-pwa-v443"') &&
+      index.includes("/app.js?v=nexus-behavior-498")
   ],
   [
     "Static HTML microphone exists before app JavaScript",
     index.includes('id="nexusPermanentMicrophoneDock"') &&
       index.includes('id="nexusPermanentMicrophoneBtn"') &&
       index.includes("Enable microphone") &&
-      index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf('/app.js?v=nexus-behavior-497') &&
+      index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf('/app.js?v=nexus-behavior-498') &&
       !/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index)
   ],
   [
diff --git a/scripts/nexus-service-worker-cache-safety-qa.js b/scripts/nexus-service-worker-cache-safety-qa.js
index 4ecbf749..2e1dbe1c 100644
--- a/scripts/nexus-service-worker-cache-safety-qa.js
+++ b/scripts/nexus-service-worker-cache-safety-qa.js
@@ -13,8 +13,8 @@ function includesAll(source, tokens, label) {
 }
 
 includesAll(sw, [
-  'const CACHE_NAME = "agrinexus-pwa-v442"',
-  'const BUILD_VERSION = "nexus-behavior-497"',
+  'const CACHE_NAME = "agrinexus-pwa-v443"',
+  'const BUILD_VERSION = "nexus-behavior-498"',
   "function isCacheableApplicationRequest",
   '["http:", "https:"].includes(url.protocol)',
   "url.origin !== self.location.origin",
@@ -49,8 +49,8 @@ assert(sw.includes(".catch(error =>"), "install cache failures must be handled")
 
 includesAll(app, [
   'const NEXUS_GENESIS_VOICE_RUNTIME_VERSION = "nexus-genesis-voice-runtime-v456"',
-  'const AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"',
-  'const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"',
+  'const AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"',
+  'const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"',
   "console.info(`[Nexus Genesis voice] ${stage}",
   "controller-initialized",
   "automatic-start-entered",
diff --git a/scripts/nexus-sms-whatsapp-provider-activation-qa.js b/scripts/nexus-sms-whatsapp-provider-activation-qa.js
index a2a4c5c2..399d3df8 100644
--- a/scripts/nexus-sms-whatsapp-provider-activation-qa.js
+++ b/scripts/nexus-sms-whatsapp-provider-activation-qa.js
@@ -104,6 +104,9 @@ assert(server.includes("communications_provider_status_answered"), "Ask Nexus sh
 assert(server.includes("noFakeSmsClaim") && server.includes("noFakeWhatsappClaim"), "Ask Nexus should avoid fake send claims");
 assert(server.includes("nexusEmailProviderStatus") && app.includes("renderNexusEmailProviderStatusCard"), "existing email provider lane should remain intact");
 assert(server.includes("nexusLiveKnowledgeAllModesQuery") && app.includes("renderNexusKnowledgeRailPanel"), "existing Live Knowledge lane should remain intact");
+assert(server.includes("nexusOpenAiNativeOwnerTestRecipient"), "Realtime communications should resolve the private owner test recipient only when explicitly requested");
+assert(server.includes("OWNER_TEST_RECIPIENT_NUMBER") && server.includes("ownerTestRecipient === true"), "owner test recipient routing should remain explicit and server-only");
+assert(server.includes("const recipient = contact.to || ownerTestRecipient"), "Realtime communications should prefer a spoken recipient and safely fall back to the explicitly requested owner test recipient");
 
 [
   "TWILIO_AUTH_TOKEN: ",
diff --git a/scripts/nexus-standard-user-home-screen-redesign-qa.js b/scripts/nexus-standard-user-home-screen-redesign-qa.js
index 4725a99a..57baa086 100644
--- a/scripts/nexus-standard-user-home-screen-redesign-qa.js
+++ b/scripts/nexus-standard-user-home-screen-redesign-qa.js
@@ -319,14 +319,14 @@ function excludes(source, token, label) {
 ].forEach(token => excludes(app, token, "Standard User home screen"));
 
 [
-  [app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"', "app build version"],
-  [app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"', "app cache version"],
-  [server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497"', "server build version"],
-  [server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"', "server cache version"],
-  [sw, 'CACHE_NAME = "agrinexus-pwa-v442"', "service worker cache"],
-  [sw, 'BUILD_VERSION = "nexus-behavior-497"', "service worker build"],
-  [index, "/styles.css?v=nexus-behavior-497", "stylesheet cache bust"],
-  [index, "/app.js?v=nexus-behavior-497", "app script cache bust"]
+  [app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"', "app build version"],
+  [app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"', "app cache version"],
+  [server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498"', "server build version"],
+  [server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"', "server cache version"],
+  [sw, 'CACHE_NAME = "agrinexus-pwa-v443"', "service worker cache"],
+  [sw, 'BUILD_VERSION = "nexus-behavior-498"', "service worker build"],
+  [index, "/styles.css?v=nexus-behavior-498", "stylesheet cache bust"],
+  [index, "/app.js?v=nexus-behavior-498", "app script cache bust"]
 ].forEach(([source, token, label]) => includes(source, token, label));
 
 assert.strictEqual(
diff --git a/scripts/nexus-system-wide-integrity-hardening-qa.js b/scripts/nexus-system-wide-integrity-hardening-qa.js
index 3c91b8bb..0812cadd 100644
--- a/scripts/nexus-system-wide-integrity-hardening-qa.js
+++ b/scripts/nexus-system-wide-integrity-hardening-qa.js
@@ -277,19 +277,19 @@ function assertEnvelope(envelope, label, correlationId) {
   ].forEach(token => assert(serverSource.includes(token), `server missing ${token}`));
   FAILURE_CATEGORIES.forEach(category => assert(serverSource.includes(category), `failure taxonomy missing ${category}`));
   [
-    "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-497\"",
-    "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v442\"",
+    "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-498\"",
+    "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v443\"",
     "const NEXUS_GENESIS_VOICE_RUNTIME_VERSION = \"nexus-genesis-voice-runtime-v456\"",
     "payload?.nexusResponse || payload?.genesisResponse"
   ].forEach(token => assert(appSource.includes(token), `app missing ${token}`));
   [
-    "const CACHE_NAME = \"agrinexus-pwa-v442\"",
-    "const BUILD_VERSION = \"nexus-behavior-497\""
+    "const CACHE_NAME = \"agrinexus-pwa-v443\"",
+    "const BUILD_VERSION = \"nexus-behavior-498\""
   ].forEach(token => assert(swSource.includes(token), `service worker missing ${token}`));
   [
-    "/app.js?v=nexus-behavior-497",
-    "/styles.css?v=nexus-behavior-497",
-    "/manifest.webmanifest?v=nexus-behavior-497"
+    "/app.js?v=nexus-behavior-498",
+    "/styles.css?v=nexus-behavior-498",
+    "/manifest.webmanifest?v=nexus-behavior-498"
   ].forEach(token => assert(indexSource.includes(token), `index missing ${token}`));
   assert(appSource.includes("startVoiceListening({ source: \"genesis-home-permission-granted-auto-start\" })"), "Genesis mic auto-start must remain intact");
   assert(appSource.includes("startRealtimeVoiceSession"), "OpenAI Realtime start must remain intact");
diff --git a/scripts/nexus-true-experience-qa-common.js b/scripts/nexus-true-experience-qa-common.js
index 1ec8a4e1..47d95bda 100644
--- a/scripts/nexus-true-experience-qa-common.js
+++ b/scripts/nexus-true-experience-qa-common.js
@@ -143,11 +143,11 @@ function assertContextualMission() {
 }
 
 function assertCacheResponsive() {
-  assert(app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-497"'), "app build version bumped");
-  assert(app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"'), "app cache version bumped");
-  assert(server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497"'), "server build version bumped");
-  assert(server.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442"'), "server cache version bumped");
-  assert(sw.includes('const CACHE_NAME = "agrinexus-pwa-v442"'), "service worker cache version bumped");
+  assert(app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-498"'), "app build version bumped");
+  assert(app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"'), "app cache version bumped");
+  assert(server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498"'), "server build version bumped");
+  assert(server.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443"'), "server cache version bumped");
+  assert(sw.includes('const CACHE_NAME = "agrinexus-pwa-v443"'), "service worker cache version bumped");
   assert(styles.includes("@media (max-width: 520px)"), "small mobile viewport styles exist");
   assert(styles.includes("@media (max-height: 640px) and (min-width: 700px)"), "short desktop viewport styles exist");
   assert(styles.includes("overflow-x: hidden"), "horizontal overflow is guarded");
diff --git a/scripts/nexus-world-demonstration-release-qa.js b/scripts/nexus-world-demonstration-release-qa.js
index de08fbaf..3e0725ec 100644
--- a/scripts/nexus-world-demonstration-release-qa.js
+++ b/scripts/nexus-world-demonstration-release-qa.js
@@ -113,21 +113,21 @@ const submitRouting = between(app, "function routeNexusCommandCenterCommunicatio
 assert(submitRouting.indexOf("handleNexusVoiceTroubleshootingCommand(command, { source })") < submitRouting.indexOf("advanceNexusOsMissionForCommand(command, { source });"), "voice status commands must run before mission planning");
 
 includesAll(index, [
-  "/manifest.webmanifest?v=nexus-behavior-497",
-  "/styles.css?v=nexus-behavior-497",
-  "/app.js?v=nexus-behavior-497"
+  "/manifest.webmanifest?v=nexus-behavior-498",
+  "/styles.css?v=nexus-behavior-498",
+  "/app.js?v=nexus-behavior-498"
 ], "index cache bust");
 includesAll(app, [
-  "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-497\";",
-  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v442\";"
+  "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-498\";",
+  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v443\";"
 ], "app cache bust");
 includesAll(sw, [
-  "const CACHE_NAME = \"agrinexus-pwa-v442\";",
-  "const BUILD_VERSION = \"nexus-behavior-497\";"
+  "const CACHE_NAME = \"agrinexus-pwa-v443\";",
+  "const BUILD_VERSION = \"nexus-behavior-498\";"
 ], "service worker cache bust");
 includesAll(server, [
-  "const AGRINEXUS_WEB_BUILD_VERSION = \"nexus-behavior-497\";",
-  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v442\";"
+  "const AGRINEXUS_WEB_BUILD_VERSION = \"nexus-behavior-498\";",
+  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v443\";"
 ], "server cache bust");
 
 assert.strictEqual(
diff --git a/server.js b/server.js
index adcdfdcb..5072d67c 100644
--- a/server.js
+++ b/server.js
@@ -55,8 +55,8 @@ const AI_MODEL = process.env.OPENAI_MODEL || "gpt-5.4-mini";
 const AI_REASONING_MODEL = process.env.OPENAI_REASONING_MODEL || process.env.OPENAI_AGENT_MODEL || AI_MODEL;
 const AI_TRANSLATION_MODEL = process.env.OPENAI_TRANSLATION_MODEL || process.env.OPENAI_AGENT_MODEL || AI_MODEL;
 const AGRINEXUS_RELEASE = "2026-06-16-operational-readiness";
-const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-497";
-const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v442";
+const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-498";
+const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v443";
 const NEXUS_GENESIS_REALTIME_RUNTIME_VERSION = "nexus-genesis-openai-agents-realtime-v3";
 const NEXUS_GENESIS_VOICE_RUNTIME_VALUES = new Set(["realtime", "disabled"]);
 const NEXUS_GENESIS_REALTIME_FALLBACK_VALUES = new Set(["blocked"]);
@@ -17537,6 +17537,13 @@ function nexusOpenAiNativeExtractContactArgs(command = "", args = {}) {
   };
 }
 
+function nexusOpenAiNativeOwnerTestRecipient(command = "", args = {}, env = process.env) {
+  const explicitlyRequested = args.ownerTestRecipient === true
+    || /\b(?:my|the|our)\s+(?:approved\s+)?owner(?:'s)?\s+test\s+(?:recipient|number|phone)\b/i.test(String(command || ""));
+  if (!explicitlyRequested) return "";
+  return sanitizePilotText(firstPresentEnvValue(env, ["OWNER_TEST_RECIPIENT_NUMBER", "TEST_RECIPIENT_NUMBER"]), 120);
+}
+
 function nexusOpenAiNativeCreateLocalReminder(db, user, common = {}, args = {}) {
   ensureNexusPilotState(db);
   const confirmed = Boolean(args.confirmed || args.confirmation);
@@ -17833,14 +17840,16 @@ async function executeNexusOpenAiNativeTool(db, user, toolName = "", args = {},
   }
   if (toolName === "nexus_communications") {
     const contact = nexusOpenAiNativeExtractContactArgs(command, args);
+    const ownerTestRecipient = nexusOpenAiNativeOwnerTestRecipient(command, args, process.env);
+    const recipient = contact.to || ownerTestRecipient;
     const channel = sanitizePilotText(args.channel || args.type || (/whatsapp/i.test(command) ? "whatsapp" : /\b(call|phone|dial)\b/i.test(command) ? "call" : /\b(email|mail)\b/i.test(command) ? "email" : "sms"), 40).toLowerCase();
     const providerResult = channel === "whatsapp"
-      ? await nexusRealProviders.twilio.sendWhatsapp({ to: contact.to, message: contact.message, confirmed: args.confirmed }, process.env)
+      ? await nexusRealProviders.twilio.sendWhatsapp({ to: recipient, message: contact.message, confirmed: args.confirmed }, process.env)
       : channel === "call"
-        ? await nexusRealProviders.twilio.startCall({ to: contact.to, message: contact.message, confirmed: args.confirmed }, process.env)
+        ? await nexusRealProviders.twilio.startCall({ to: recipient, message: contact.message, confirmed: args.confirmed }, process.env)
         : channel === "email"
           ? await nexusRealProviders.email.send({ to: contact.to, subject: contact.subject, text: contact.message, confirmed: args.confirmed }, process.env)
-          : await nexusRealProviders.twilio.sendSms({ to: contact.to, message: contact.message, confirmed: args.confirmed }, process.env);
+          : await nexusRealProviders.twilio.sendSms({ to: recipient, message: contact.message, confirmed: args.confirmed }, process.env);
     return nexusOpenAiNativeProviderToolResult(db, { ...common, capability: "communications" }, providerResult);
   }
   if (toolName === "nexus_calendar") {
