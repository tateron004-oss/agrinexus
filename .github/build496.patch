From 2a23561e48db747a297739db629738250ce564bd Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sat, 25 Jul 2026 20:17:07 -0400
Subject: [PATCH] Activate Google Cloud Translation through Nexus voice

---
 .../nexus-production-voice-certification.yml  |   9 +-
 package.json                                  |   1 +
 public/app.js                                 |   4 +-
 public/index.html                             |   8 +-
 public/sw.js                                  |   4 +-
 ...exus-companion-voice-activation-flow-qa.js |   2 +-
 ...versation-workflow-transition-engine-qa.js |   2 +-
 .../nexus-endgame-production-platform-qa.js   |   6 +-
 scripts/nexus-experience-polish-layer-qa.js   |   2 +-
 ...xus-genesis-browser-voice-acceptance-qa.js |   2 +-
 ...xus-genesis-live-provider-browser-smoke.js |  30 +++-
 ...s-genesis-persistent-voice-lifecycle-qa.js |   6 +-
 ...enesis-realtime-production-migration-qa.js |   4 +-
 ...sis-realtime-repeated-turn-lifecycle-qa.js |   2 +-
 .../nexus-genesis-truthful-voice-state-qa.js  |  18 +--
 ...s-genesis-visible-product-acceptance-qa.js |  18 +--
 .../nexus-genesis-voice-e2e-software-qa.js    |   6 +-
 ...exus-genesis-voice-native-front-door-qa.js |   8 +-
 ...us-google-cloud-translation-provider-qa.js |  73 +++++++++
 ...internet-resource-assistant-platform-qa.js |   6 +-
 ...s-internet-resource-provider-pathway-qa.js |   2 +-
 .../nexus-layered-internet-intelligence-qa.js |   8 +-
 scripts/nexus-live-knowledge-retrieval-qa.js  |   8 +-
 scripts/nexus-panel-tab-click-repair-qa.js    |  12 +-
 ...xus-platform-predictive-intelligence-qa.js |   4 +-
 scripts/nexus-production-audio-pipeline-qa.js |   8 +-
 scripts/nexus-production-platform-rails-qa.js |   4 +-
 ...nexus-realtime-microphone-visibility-qa.js |  12 +-
 .../nexus-service-worker-cache-safety-qa.js   |   8 +-
 ...s-standard-user-home-screen-redesign-qa.js |  16 +-
 ...exus-system-wide-integrity-hardening-qa.js |  14 +-
 scripts/nexus-true-experience-qa-common.js    |  10 +-
 .../nexus-world-demonstration-release-qa.js   |  18 +--
 scripts/qa-suite.js                           |   2 +
 server.js                                     |  45 ++++--
 server/google-cloud-translation-provider.js   | 148 ++++++++++++++++++
 36 files changed, 400 insertions(+), 130 deletions(-)
 create mode 100644 scripts/nexus-google-cloud-translation-provider-qa.js
 create mode 100644 server/google-cloud-translation-provider.js

diff --git a/.github/workflows/nexus-production-voice-certification.yml b/.github/workflows/nexus-production-voice-certification.yml
index c91e3b2..5617b99 100644
--- a/.github/workflows/nexus-production-voice-certification.yml
+++ b/.github/workflows/nexus-production-voice-certification.yml
@@ -11,6 +11,7 @@ on:
       - "package.json"
       - "scripts/qa-suite.js"
       - "scripts/nexus-youtube-data-api-live-provider-qa.js"
+      - "scripts/nexus-google-cloud-translation-provider-qa.js"
       - "scripts/nexus-genesis-live-provider-browser-smoke.js"
       - ".github/workflows/nexus-production-voice-certification.yml"
 
@@ -28,7 +29,7 @@ jobs:
     timeout-minutes: 20
     env:
       NEXUS_LIVE_BASE_URL: https://agrinexus-platform.onrender.com
-      NEXUS_LIVE_EXPECTED_TURNS: "13"
+      NEXUS_LIVE_EXPECTED_TURNS: "14"
       NEXUS_LIVE_REQUIRE_WORKSPACES: "1"
       NEXUS_LIVE_REQUIRED_INTERRUPTION_COUNT: "0"
       CHROME_PATH: /usr/bin/google-chrome
@@ -87,11 +88,11 @@ jobs:
         shell: bash
         run: |
           mkdir -p output/nexus-certification
-          node -e 'const fs=require("fs"); const status=process.argv[1]; const sha=process.argv[2]; const p="/tmp/nexus-voice-result.log"; const log=fs.existsSync(p)?fs.readFileSync(p,"utf8"):""; const tail=log.trim().split(/\\r?\\n/).filter(Boolean).slice(-20); fs.writeFileSync(`output/nexus-certification/live-${sha}-result.json`, JSON.stringify({build:"nexus-behavior-495",workflowCommit:sha,outcome:status,recordedAt:new Date().toISOString(),diagnosticTail:tail},null,2)+"\\n")' "${{ steps.voice.outcome }}" "${{ github.sha }}"
+          node -e 'const fs=require("fs"); const status=process.argv[1]; const sha=process.argv[2]; const p="/tmp/nexus-voice-result.log"; const log=fs.existsSync(p)?fs.readFileSync(p,"utf8"):""; const tail=log.trim().split(/\\r?\\n/).filter(Boolean).slice(-20); fs.writeFileSync(`output/nexus-certification/live-${sha}-result.json`, JSON.stringify({build:"nexus-behavior-496",workflowCommit:sha,outcome:status,recordedAt:new Date().toISOString(),diagnosticTail:tail},null,2)+"\\n")' "${{ steps.voice.outcome }}" "${{ github.sha }}"
           git config user.name "github-actions[bot]"
           git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
           git add output/nexus-certification
-          git commit -m "Record Nexus build 495 spoken certification: ${{ steps.voice.outcome }}" || true
+          git commit -m "Record Nexus build 496 spoken certification: ${{ steps.voice.outcome }}" || true
           git pull --rebase origin main
           git push origin HEAD:main
 
@@ -110,4 +111,4 @@ jobs:
         if: always() && steps.voice.outcome != 'success'
         run: exit 1
 
-# Build 495 fresh-run spoken certification trigger 2026-07-24T22:29:00-07:00.
+# Build 496 Google Cloud Translation spoken certification trigger 2026-07-25.
diff --git a/package.json b/package.json
index 194f596..d0f120e 100644
--- a/package.json
+++ b/package.json
@@ -833,6 +833,7 @@
     "qa:nexus-rp6-shipment-tracking-real-provider-activation": "node scripts/nexus-rp6-shipment-tracking-real-provider-activation-qa.js",
     "qa:nexus-rp7-music-media-real-provider-activation": "node scripts/nexus-rp7-music-media-real-provider-activation-qa.js",
     "qa:nexus-youtube-data-api-live-provider": "node scripts/nexus-youtube-data-api-live-provider-qa.js",
+    "qa:nexus-google-cloud-translation-provider": "node scripts/nexus-google-cloud-translation-provider-qa.js",
     "qa:nexus-rp8-unified-provider-live-mock-skip-matrix": "node scripts/nexus-rp8-unified-provider-live-mock-skip-matrix-qa.js",
     "qa:nexus-rp9-controlled-assistant-preview-live-provider-sweep": "node scripts/nexus-rp9-controlled-assistant-preview-live-provider-sweep-qa.js",
     "qa:nexus-rp10-standard-user-live-preview-readiness-validation": "node scripts/nexus-rp10-standard-user-live-preview-readiness-validation-qa.js",
diff --git a/public/app.js b/public/app.js
index d4c6dc0..4ef3ce2 100644
--- a/public/app.js
+++ b/public/app.js
@@ -1343,8 +1343,8 @@ const nexusProductIdentity = Object.freeze({
 });
 const assistantFullName = "AgriNexus";
 const assistantShortName = "Nexus";
-const AGRINEXUS_BUILD_VERSION = "nexus-behavior-495";
-const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440";
+const AGRINEXUS_BUILD_VERSION = "nexus-behavior-496";
+const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441";
 const VOICE_RESTART_DELAY_MS = 320;
 const VOICE_UI_FOCUS_DELAY_MS = 80;
 const VOICE_ATTENTION_DELAY_MS = 900;
diff --git a/public/index.html b/public/index.html
index 52cecbd..9f02fad 100644
--- a/public/index.html
+++ b/public/index.html
@@ -9,11 +9,11 @@
   <meta name="mobile-web-app-capable" content="yes">
   <meta name="apple-mobile-web-app-title" content="Nexus Genesis | AgriNexus">
   <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
-  <link rel="manifest" href="/manifest.webmanifest?v=nexus-behavior-495">
+  <link rel="manifest" href="/manifest.webmanifest?v=nexus-behavior-496">
   <link rel="icon" href="/icons/agri-nexus-192.png" type="image/png">
   <link rel="apple-touch-icon" href="/icons/agri-nexus-192.png">
   <link rel="stylesheet" href="/vendor/leaflet/leaflet.css?v=1.9.4">
-  <link rel="stylesheet" href="/styles.css?v=nexus-behavior-495">
+  <link rel="stylesheet" href="/styles.css?v=nexus-behavior-496">
   <script type="importmap">
     {
       "imports": {
@@ -2227,9 +2227,9 @@
   <script src="/nexus-genesis-conversational-mode-orchestrator.js?v=nexus-genesis-conversational-mode-orchestrator-2"></script>
   <script src="/nexus-os-agrinexus-deployment-profile.js?v=nexus-os-agrinexus-deployment-1"></script>
   <script src="/nexus-os-health-workforce-safety-pack.js?v=nexus-os-health-workforce-safety-1"></script>
-  <script src="/nexus-genesis-voice-runtime-manager.js?v=nexus-behavior-495"></script>
+  <script src="/nexus-genesis-voice-runtime-manager.js?v=nexus-behavior-496"></script>
   <script src="/browser-action-controller.js?v=nexus-action-controller-1"></script>
-  <script src="/app.js?v=nexus-behavior-495"></script>
+  <script src="/app.js?v=nexus-behavior-496"></script>
   <script src="/nexus-voice-demo-shell.js?v=nexus-behavior-305"></script>
 </body>
 </html>
diff --git a/public/sw.js b/public/sw.js
index 56b1715..138aaa5 100644
--- a/public/sw.js
+++ b/public/sw.js
@@ -1,5 +1,5 @@
-const CACHE_NAME = "agrinexus-pwa-v440";
-const BUILD_VERSION = "nexus-behavior-495";
+const CACHE_NAME = "agrinexus-pwa-v441";
+const BUILD_VERSION = "nexus-behavior-496";
 const APP_SHELL = [
   "/",
   "/index.html",
diff --git a/scripts/nexus-companion-voice-activation-flow-qa.js b/scripts/nexus-companion-voice-activation-flow-qa.js
index 0ab50eb..5d2eb13 100644
--- a/scripts/nexus-companion-voice-activation-flow-qa.js
+++ b/scripts/nexus-companion-voice-activation-flow-qa.js
@@ -31,7 +31,7 @@ assert(!app.includes("Nexus is blocked from executing externally."), "raw blocke
 assert(app.includes("I can help here, but live external actions need a connected service."), "friendly external-action limitation must be present");
 assert(index.includes('<meta name="mobile-web-app-capable" content="yes">'), "index should include modern mobile-web-app-capable metadata to avoid browser warning");
 assert(!index.includes('name="apple-mobile-web-app-capable"'), "index should avoid deprecated apple-mobile-web-app-capable warning");
-assert(index.includes("/manifest.webmanifest?v=nexus-behavior-495"), "index should version the manifest link to avoid stale manifest warnings");
+assert(index.includes("/manifest.webmanifest?v=nexus-behavior-496"), "index should version the manifest link to avoid stale manifest warnings");
 assert(manifest.includes('"enctype": "application/x-www-form-urlencoded"'), "manifest share target should specify enctype to avoid browser warning");
 
 includesAll(app, [
diff --git a/scripts/nexus-conversation-workflow-transition-engine-qa.js b/scripts/nexus-conversation-workflow-transition-engine-qa.js
index 4352cc0..76c9bd8 100644
--- a/scripts/nexus-conversation-workflow-transition-engine-qa.js
+++ b/scripts/nexus-conversation-workflow-transition-engine-qa.js
@@ -208,7 +208,7 @@ assert(reminderProposal.options.some(workflow => workflow.workflowId === "daily-
 assert(/no scheduled reminder receipt/i.test(reminderProposal.options.find(workflow => workflow.workflowId === "daily-life.reminder-proposal").receiptBehavior), "reminder proposal does not fake scheduling");
 
 includes(index, "/nexus-conversation-workflow-transition-engine.js", "index script loading");
-includes(index, "/app.js?v=nexus-behavior-495", "app cache version bump");
+includes(index, "/app.js?v=nexus-behavior-496", "app cache version bump");
 includes(app, "nexusConversationWorkflowTransitionState", "app state");
 includes(app, "function handleNexusConversationWorkflowTransitionCommand", "app handler");
 includes(app, "renderNexusConversationWorkflowTransitionCard()", "same-conversation renderer");
diff --git a/scripts/nexus-endgame-production-platform-qa.js b/scripts/nexus-endgame-production-platform-qa.js
index 2b36a0a..402affc 100644
--- a/scripts/nexus-endgame-production-platform-qa.js
+++ b/scripts/nexus-endgame-production-platform-qa.js
@@ -226,9 +226,9 @@ check("multilingual status avoids full certification claim", hasAll(server, [
   "noFullCertificationClaim"
 ]));
 
-check("service worker/cache bumped", server.includes("nexus-behavior-495")
-  && app.includes("nexus-behavior-495")
-  && sw.includes("agrinexus-pwa-v440"));
+check("service worker/cache bumped", server.includes("nexus-behavior-496")
+  && app.includes("nexus-behavior-496")
+  && sw.includes("agrinexus-pwa-v441"));
 
 check("package alias exists", pkg.scripts["qa:nexus-endgame-production-platform"] === "node scripts/nexus-endgame-production-platform-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-endgame-production-platform-qa.js"));
diff --git a/scripts/nexus-experience-polish-layer-qa.js b/scripts/nexus-experience-polish-layer-qa.js
index 1683585..fe0d6b1 100644
--- a/scripts/nexus-experience-polish-layer-qa.js
+++ b/scripts/nexus-experience-polish-layer-qa.js
@@ -169,7 +169,7 @@ check("safe suites include experience polish QA", hasAll(qaSuite, [
 ]));
 
 check("build token bumped with frontend change", hasAll(app, [
-  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"'
+  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"'
 ]));
 
 if (failures) {
diff --git a/scripts/nexus-genesis-browser-voice-acceptance-qa.js b/scripts/nexus-genesis-browser-voice-acceptance-qa.js
index fced315..3dc56f2 100644
--- a/scripts/nexus-genesis-browser-voice-acceptance-qa.js
+++ b/scripts/nexus-genesis-browser-voice-acceptance-qa.js
@@ -131,7 +131,7 @@ assert(
   qaSuite.includes('"scripts/nexus-genesis-browser-voice-acceptance-qa.js"'),
   "voice/all-safe suite must include browser voice acceptance QA"
 );
-assert(index.includes("/app.js?v=nexus-behavior-495"), "index must bump app.js version so browser voice fixes load");
+assert(index.includes("/app.js?v=nexus-behavior-496"), "index must bump app.js version so browser voice fixes load");
 
 includesAll(acceptanceDoc, [
   "Nexus Genesis Real Browser Voice and Companion Acceptance",
diff --git a/scripts/nexus-genesis-live-provider-browser-smoke.js b/scripts/nexus-genesis-live-provider-browser-smoke.js
index 1d0b7c9..ec704b0 100644
--- a/scripts/nexus-genesis-live-provider-browser-smoke.js
+++ b/scripts/nexus-genesis-live-provider-browser-smoke.js
@@ -22,7 +22,8 @@ const spokenJourneys = [
   { workspace: "media", words: ["YouTube video found", "maize"], command: "Nexus, use YouTube to show me how to plant maize." },
   { workspace: "reminders", words: ["Reminders"], command: "Nexus, open Reminders." },
   { workspace: "offline", words: ["Offline", "Queue"], command: "Nexus, open the Offline Queue." },
-  { workspace: "live-knowledge", words: ["climate-smart", "sources"], command: "Nexus, use the internet to research current climate-smart agriculture information and show sources." }
+  { workspace: "live-knowledge", words: ["climate-smart", "sources"], command: "Nexus, use the internet to research current climate-smart agriculture information and show sources." },
+  { workspace: null, words: [], provider: "google-cloud-translation", command: "Nexus, change language to Swahili and tell me good morning farmer." }
 ];
 const browserCandidates = process.platform === "win32"
   ? ["C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe", "C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe"]
@@ -246,6 +247,21 @@ async function main() {
         EventTarget.prototype.addEventListener = function(type, listener, options) { if (type === "click" && this.id === "nexusPermanentMicrophoneBtn") this.dataset.nexusMicBound = "true"; return nexusOriginalAddEventListener.call(this, type, listener, options); };
         window.__NEXUS_VOICE_ACCEPTANCE_EVENTS__ = [];
         window.__NEXUS_WORKSPACE_ACKS__ = [];
+        window.__NEXUS_TRANSLATION_TOOL_RESULTS__ = [];
+        const nexusOriginalFetch = window.fetch.bind(window);
+        window.fetch = async (...args) => {
+          const response = await nexusOriginalFetch(...args);
+          const requestUrl = String(args[0]?.url || args[0] || '');
+          if (requestUrl.includes('/api/voice/realtime/tool')) {
+            response.clone().json().then(payload => {
+              if (payload?.translationProvider) window.__NEXUS_TRANSLATION_TOOL_RESULTS__.push({
+                provider: String(payload.translationProvider),
+                response: String(payload.response || '')
+              });
+            }).catch(() => {});
+          }
+          return response;
+        };
         window.addEventListener('genesis.workspace.acknowledged', event => window.__NEXUS_WORKSPACE_ACKS__.push({ ...event.detail, visibleText: document.body?.innerText || '' }));
         window.__NEXUS_RESOURCE_TRACKER__ = { streams: new Set(), tracks: new Set(), audioContexts: new Set(), peers: new Set() };
         window.__NEXUS_RESOURCE_COUNTS__ = () => ({
@@ -341,7 +357,7 @@ async function main() {
       const lifecycleEvents = window.NexusGenesisVoiceLifecycleDiagnostics?.().events || [];
       const workspaceAcks = window.__NEXUS_WORKSPACE_ACKS__ || [];
       const requiredJourneys = ${JSON.stringify(spokenJourneys)};
-      const workspaceResults = requiredJourneys.map(journey => {
+      const workspaceResults = requiredJourneys.filter(journey => journey.workspace).map(journey => {
         const normalizeVisibleWords = value => String(value || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim();
         const candidates = workspaceAcks.filter(item => item.workspace === journey.workspace && item.opened === true && item.visible === true && item.verified === true);
         const wordsVisibleForAck = item => {
@@ -354,6 +370,8 @@ async function main() {
         return { workspace: journey.workspace, acknowledged: Boolean(ack), requestId: ack?.requestId || '', populatedFields: ack?.populatedFields || [], microphoneActive: ack?.microphoneActive === true, realtimeConnected: ack?.realtimeConnected === true, wordsVisible: Boolean(ack && wordsVisibleForAck(ack)) };
       });
       const workspacesSatisfied = !${requireWorkspaces} || workspaceResults.every(item => item.acknowledged && item.requestId && item.populatedFields.length && item.microphoneActive && item.realtimeConnected && item.wordsVisible);
+      const translationResults = window.__NEXUS_TRANSLATION_TOOL_RESULTS__ || [];
+      const translationSatisfied = translationResults.some(item => item.provider === 'google-cloud-translation' && item.response.trim());
       const lifecycleInterruptionCount = lifecycleEvents.filter(event => /interrupt|cancel-requested/.test(String(event.eventName || ''))).length;
       const interruptionSatisfied = interruptionCount + lifecycleInterruptionCount >= ${requiredInterruptions};
       window.__NEXUS_ACCEPTANCE_SNAPSHOT__ = {
@@ -367,10 +385,12 @@ async function main() {
         lifecycleInterruptionCount,
         interruptionSatisfied,
         workspacesSatisfied,
+        translationSatisfied,
+        translationResults,
         workspaceAckCount: workspaceAcks.length,
         workspaceResults
       };
-      if (!speechStarted || !responseDone || !modelAudio || !interruptionSatisfied || !workspacesSatisfied) return null;
+      if (!speechStarted || !responseDone || !modelAudio || !interruptionSatisfied || !workspacesSatisfied || !translationSatisfied) return null;
       return {
         speechStarted,
         responseDone,
@@ -387,7 +407,8 @@ async function main() {
         expectedTurns: ${expectedTurns},
         requiredInterruptions: ${requiredInterruptions},
         lifecycle: window.NexusGenesisVoiceLifecycleDiagnostics?.().currentInvariant || null,
-        workspaceResults
+        workspaceResults,
+        translationResults
       };
     })()`), Math.max(180000, expectedTurns * 30000), "synthetic spoken turn and model response");
 
@@ -417,6 +438,7 @@ async function main() {
       expectedWordObserved: evidence.expectedWordObserved,
       lifecycleInvariant: evidence.lifecycle?.ok === true,
       workspaceResults: evidence.workspaceResults,
+      translationResults: evidence.translationResults,
       browserSecretLoggingDetected: false,
       eventCount: evidence.eventCount
       ,expectedTurns: evidence.expectedTurns,
diff --git a/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js b/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
index 6b3f2a5..0f6788a 100644
--- a/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
+++ b/scripts/nexus-genesis-persistent-voice-lifecycle-qa.js
@@ -21,16 +21,16 @@ function notIncludes(source, needle, label) {
   assert(!source.includes(needle), `${label} must not include: ${needle}`);
 }
 
-["nexus-behavior-495", "agrinexus-pwa-v440"].forEach(marker => {
+["nexus-behavior-496", "agrinexus-pwa-v441"].forEach(marker => {
   includes(app, marker, `app marker ${marker}`);
   includes(server, marker, `server marker ${marker}`);
   includes(sw, marker, `service worker marker ${marker}`);
 });
-includes(index, "nexus-behavior-495", "index behavior marker nexus-behavior-495");
+includes(index, "nexus-behavior-496", "index behavior marker nexus-behavior-496");
 
 includes(index, 'id="nexusPermanentMicrophoneBtn"', "static microphone button");
 includes(index, "Enable microphone", "static microphone initial label");
-assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-495"), "microphone button must appear before app.js");
+assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-496"), "microphone button must appear before app.js");
 assert(!/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index), "static microphone button must not be hidden or disabled");
 
 [
diff --git a/scripts/nexus-genesis-realtime-production-migration-qa.js b/scripts/nexus-genesis-realtime-production-migration-qa.js
index 6ebe228..d75366b 100644
--- a/scripts/nexus-genesis-realtime-production-migration-qa.js
+++ b/scripts/nexus-genesis-realtime-production-migration-qa.js
@@ -168,8 +168,8 @@ const scenarios = [
 ];
 
 const requirements = [
-  ["Build markers bumped", server.includes("nexus-behavior-495") && app.includes("nexus-behavior-495") && sw.includes("nexus-behavior-495") && index.includes("nexus-behavior-495")],
-  ["PWA markers bumped", server.includes("agrinexus-pwa-v440") && app.includes("agrinexus-pwa-v440") && sw.includes("agrinexus-pwa-v440")],
+  ["Build markers bumped", server.includes("nexus-behavior-496") && app.includes("nexus-behavior-496") && sw.includes("nexus-behavior-496") && index.includes("nexus-behavior-496")],
+  ["PWA markers bumped", server.includes("agrinexus-pwa-v441") && app.includes("agrinexus-pwa-v441") && sw.includes("agrinexus-pwa-v441")],
   ["Genesis runtime marker bumped", app.includes("nexus-genesis-voice-runtime-v456")],
   ["Realtime runtime version exists", server.includes("NEXUS_GENESIS_REALTIME_RUNTIME_VERSION") && app.includes("NEXUS_GENESIS_REALTIME_RUNTIME_VERSION")],
   ["Server runtime values", server.includes('const NEXUS_GENESIS_VOICE_RUNTIME_VALUES = new Set(["realtime", "disabled"])')],
diff --git a/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js b/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
index 86a0352..f40f6d8 100644
--- a/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
+++ b/scripts/nexus-genesis-realtime-repeated-turn-lifecycle-qa.js
@@ -66,7 +66,7 @@ includes(server, "create_response: true", "server VAD repeated response creation
 includes(server, "interrupt_response: true", "server VAD interruption support");
 includes(server, "silence_duration_ms: Number(env.OPENAI_REALTIME_SILENCE_DURATION_MS || 700)", "server VAD silence duration retained");
 
-["nexus-behavior-495", "agrinexus-pwa-v440"].forEach(marker => {
+["nexus-behavior-496", "agrinexus-pwa-v441"].forEach(marker => {
   includes(app, marker, `app marker ${marker}`);
   includes(server, marker, `server marker ${marker}`);
   includes(sw, marker, `service worker marker ${marker}`);
diff --git a/scripts/nexus-genesis-truthful-voice-state-qa.js b/scripts/nexus-genesis-truthful-voice-state-qa.js
index bfd323d..09179a7 100644
--- a/scripts/nexus-genesis-truthful-voice-state-qa.js
+++ b/scripts/nexus-genesis-truthful-voice-state-qa.js
@@ -42,22 +42,22 @@ const speechSynthesis = sectionBetween(app, "function runNexusSpeechSynthesisCon
 const speechResume = sectionBetween(app, "function resumeVoiceListeningAfterSpeech", "function stopVoicePlayback", "speech restart");
 
 includesAll(app, [
-  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"',
-  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"',
+  'AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"',
+  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"',
   'NEXUS_GENESIS_VOICE_RUNTIME_VERSION = "nexus-genesis-voice-runtime-v456"'
 ], "app build");
 includesAll(index, [
-  "/manifest.webmanifest?v=nexus-behavior-495",
-  "/styles.css?v=nexus-behavior-495",
-  "/app.js?v=nexus-behavior-495"
+  "/manifest.webmanifest?v=nexus-behavior-496",
+  "/styles.css?v=nexus-behavior-496",
+  "/app.js?v=nexus-behavior-496"
 ], "index build");
 includesAll(server, [
-  'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495"',
-  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"'
+  'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496"',
+  'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"'
 ], "server build");
 includesAll(sw, [
-  'CACHE_NAME = "agrinexus-pwa-v440"',
-  'BUILD_VERSION = "nexus-behavior-495"'
+  'CACHE_NAME = "agrinexus-pwa-v441"',
+  'BUILD_VERSION = "nexus-behavior-496"'
 ], "service worker build");
 
 includesAll(truthfulState, [
diff --git a/scripts/nexus-genesis-visible-product-acceptance-qa.js b/scripts/nexus-genesis-visible-product-acceptance-qa.js
index d803a3d..8f5cac7 100644
--- a/scripts/nexus-genesis-visible-product-acceptance-qa.js
+++ b/scripts/nexus-genesis-visible-product-acceptance-qa.js
@@ -59,15 +59,15 @@ includes(server, 'edition: "genesis"', "server edition");
 assert.strictEqual(registry.productName, "Nexus Genesis | AgriNexus", "tool registry product identity");
 assert.strictEqual(registry.edition, "genesis", "tool registry edition");
 
-includes(index, "/manifest.webmanifest?v=nexus-behavior-495", "manifest cache marker");
-includes(index, "/styles.css?v=nexus-behavior-495", "stylesheet cache marker");
-includes(index, "/app.js?v=nexus-behavior-495", "app cache marker");
-includes(app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"', "app build marker");
-includes(app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"', "app PWA cache marker");
-includes(server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495"', "server build marker");
-includes(server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"', "server PWA cache marker");
-includes(sw, 'CACHE_NAME = "agrinexus-pwa-v440"', "service worker cache marker");
-includes(sw, 'BUILD_VERSION = "nexus-behavior-495"', "service worker build marker");
+includes(index, "/manifest.webmanifest?v=nexus-behavior-496", "manifest cache marker");
+includes(index, "/styles.css?v=nexus-behavior-496", "stylesheet cache marker");
+includes(index, "/app.js?v=nexus-behavior-496", "app cache marker");
+includes(app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"', "app build marker");
+includes(app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"', "app PWA cache marker");
+includes(server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496"', "server build marker");
+includes(server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"', "server PWA cache marker");
+includes(sw, 'CACHE_NAME = "agrinexus-pwa-v441"', "service worker cache marker");
+includes(sw, 'BUILD_VERSION = "nexus-behavior-496"', "service worker build marker");
 includes(server, 'if (!user && url.pathname !== "/api/config" && !boundedGenesisVoiceGuestRoutes.has(url.pathname))', "public redacted config route and bounded voice routes must not trigger guest console 401");
 
 excludes(index, "data-nexus-genesis-orb-entry", "index orb entry");
diff --git a/scripts/nexus-genesis-voice-e2e-software-qa.js b/scripts/nexus-genesis-voice-e2e-software-qa.js
index cd21804..840f136 100644
--- a/scripts/nexus-genesis-voice-e2e-software-qa.js
+++ b/scripts/nexus-genesis-voice-e2e-software-qa.js
@@ -35,8 +35,8 @@ function countMatches(source, regex) {
   return Array.from(source.matchAll(regex)).length;
 }
 
-const build = "nexus-behavior-495";
-const cache = "agrinexus-pwa-v440";
+const build = "nexus-behavior-496";
+const cache = "agrinexus-pwa-v441";
 
 [
   ["app", app],
@@ -50,7 +50,7 @@ includes(index, build, "index build marker");
 
 assert.equal(countMatches(index, /id="nexusPermanentMicrophoneBtn"/g), 1, "static HTML must contain exactly one permanent microphone button");
 assert.equal(countMatches(index, /id="nexusPermanentMicrophoneDock"/g), 1, "static HTML must contain exactly one permanent microphone dock");
-assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-495"), "microphone button must exist before app.js executes");
+assert(index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf("/app.js?v=nexus-behavior-496"), "microphone button must exist before app.js executes");
 assert(!/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index), "microphone button must not be hidden or disabled in HTML");
 includes(index, "Enable microphone", "initial microphone label");
 includes(index, 'aria-describedby="nexusPermanentMicrophoneStatus"', "accessible microphone status binding");
diff --git a/scripts/nexus-genesis-voice-native-front-door-qa.js b/scripts/nexus-genesis-voice-native-front-door-qa.js
index 8ba3305..ddb81ef 100644
--- a/scripts/nexus-genesis-voice-native-front-door-qa.js
+++ b/scripts/nexus-genesis-voice-native-front-door-qa.js
@@ -141,10 +141,10 @@ includesAll(speechResume, [
   "recognition-restart-requested"
 ], "speech restart");
 
-includesAll(index, ["/app.js?v=nexus-behavior-495", "/styles.css?v=nexus-behavior-495"], "index cache");
-includesAll(app, ["nexus-behavior-495", "agrinexus-pwa-v440", "nexus-genesis-voice-runtime-v456"], "app cache");
-includesAll(server, ["nexus-behavior-495", "agrinexus-pwa-v440"], "server cache");
-includesAll(sw, ["nexus-behavior-495", "agrinexus-pwa-v440"], "service worker cache");
+includesAll(index, ["/app.js?v=nexus-behavior-496", "/styles.css?v=nexus-behavior-496"], "index cache");
+includesAll(app, ["nexus-behavior-496", "agrinexus-pwa-v441", "nexus-genesis-voice-runtime-v456"], "app cache");
+includesAll(server, ["nexus-behavior-496", "agrinexus-pwa-v441"], "server cache");
+includesAll(sw, ["nexus-behavior-496", "agrinexus-pwa-v441"], "service worker cache");
 
 assert(
   packageJson.scripts["qa:nexus-genesis-voice-native-front-door"] === "node scripts/nexus-genesis-voice-native-front-door-qa.js",
diff --git a/scripts/nexus-google-cloud-translation-provider-qa.js b/scripts/nexus-google-cloud-translation-provider-qa.js
new file mode 100644
index 0000000..e4085ae
--- /dev/null
+++ b/scripts/nexus-google-cloud-translation-provider-qa.js
@@ -0,0 +1,73 @@
+"use strict";
+
+const assert = require("node:assert/strict");
+const crypto = require("node:crypto");
+const provider = require("../server/google-cloud-translation-provider.js");
+
+function response(payload, status = 200) {
+  return {
+    ok: status >= 200 && status < 300,
+    status,
+    async json() {
+      return payload;
+    }
+  };
+}
+
+async function run() {
+  const { privateKey } = crypto.generateKeyPairSync("rsa", { modulusLength: 2048 });
+  const privateKeyPem = privateKey.export({ type: "pkcs8", format: "pem" });
+  const credentials = {
+    type: "service_account",
+    project_id: "agrinexus-genesis-test",
+    private_key_id: "not-a-production-key",
+    private_key: privateKeyPem,
+    client_email: "agrinexus-translation@agrinexus-genesis-test.iam.gserviceaccount.com",
+    token_uri: provider.GOOGLE_OAUTH_TOKEN_URL
+  };
+  const env = { GOOGLE_TRANSLATION_CREDENTIALS_JSON: JSON.stringify(credentials) };
+  const calls = [];
+  const fetchImpl = async (url, options) => {
+    calls.push({ url: String(url), options });
+    if (String(url) === provider.GOOGLE_OAUTH_TOKEN_URL) {
+      assert.match(String(options.body), /grant_type=/);
+      assert.doesNotMatch(String(options.body), /PRIVATE KEY/);
+      return response({ access_token: "short-lived-oauth-token", expires_in: 3600 });
+    }
+    assert.equal(String(url), "https://translation.googleapis.com/v3/projects/agrinexus-genesis-test/locations/global:translateText");
+    assert.equal(options.headers.authorization, "Bearer short-lived-oauth-token");
+    const body = JSON.parse(options.body);
+    assert.deepEqual(body.contents, ["Good morning farmer"]);
+    assert.equal(body.sourceLanguageCode, "en");
+    assert.equal(body.targetLanguageCode, "sw");
+    return response({ translations: [{ translatedText: "Habari za asubuhi mkulima" }] });
+  };
+
+  assert.equal(provider.isConfigured(env), true);
+  const result = await provider.translateText({
+    text: "Good morning farmer",
+    sourceLanguage: "en",
+    targetLanguage: "sw",
+    env,
+    fetchImpl
+  });
+  assert.equal(result.provider, "google-cloud-translation");
+  assert.equal(result.translatedText, "Habari za asubuhi mkulima");
+  assert.equal(calls.length, 2);
+  assert.doesNotMatch(JSON.stringify(result), /private_key|short-lived-oauth-token|PRIVATE KEY/);
+  assert.equal(provider.isConfigured({}), false);
+  assert.throws(
+    () => provider.parseCredentials({ GOOGLE_TRANSLATION_CREDENTIALS_JSON: "not-json" }),
+    error => error.code === "invalid-credentials-json"
+  );
+  console.log("[nexus-google-cloud-translation-provider-qa] passed");
+}
+
+if (require.main === module) {
+  run().catch(error => {
+    console.error(error?.stack || error);
+    process.exitCode = 1;
+  });
+}
+
+module.exports = Object.freeze({ run });
diff --git a/scripts/nexus-internet-resource-assistant-platform-qa.js b/scripts/nexus-internet-resource-assistant-platform-qa.js
index ff4dc94..eab5133 100644
--- a/scripts/nexus-internet-resource-assistant-platform-qa.js
+++ b/scripts/nexus-internet-resource-assistant-platform-qa.js
@@ -224,9 +224,9 @@ check("Standard User first screen remains conversation-first and uncluttered", h
   'data-standard-user-startup-visible="false" hidden aria-hidden="true"'
 ]) && modeLabels.every(label => app.includes(label)) && !app.includes("workflow tab wall"));
 
-check("service worker/cache bumped for frontend change", hasAll(server, ["nexus-behavior-495", "agrinexus-pwa-v440"])
-  && hasAll(app, ["nexus-behavior-495", "agrinexus-pwa-v440"])
-  && hasAll(sw, ["nexus-behavior-495", "agrinexus-pwa-v440"]));
+check("service worker/cache bumped for frontend change", hasAll(server, ["nexus-behavior-496", "agrinexus-pwa-v441"])
+  && hasAll(app, ["nexus-behavior-496", "agrinexus-pwa-v441"])
+  && hasAll(sw, ["nexus-behavior-496", "agrinexus-pwa-v441"]));
 
 check("package alias exists", pkg.scripts["qa:nexus-internet-resource-assistant-platform"] === "node scripts/nexus-internet-resource-assistant-platform-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-internet-resource-assistant-platform-qa.js"));
diff --git a/scripts/nexus-internet-resource-provider-pathway-qa.js b/scripts/nexus-internet-resource-provider-pathway-qa.js
index b100c15..b1a0226 100644
--- a/scripts/nexus-internet-resource-provider-pathway-qa.js
+++ b/scripts/nexus-internet-resource-provider-pathway-qa.js
@@ -143,7 +143,7 @@ check("standard user first screen remains icon-first", [
 
 check("false live claims remain blocked", !/\b(Nexus|we|I)\s+(sent|called|processed|dispatched|fulfilled|connected you to a live provider|completed payment|placed a call)\b/i.test(app + server));
 
-check("service worker/cache bumped", server.includes("nexus-behavior-495") && app.includes("nexus-behavior-495") && sw.includes("agrinexus-pwa-v440"));
+check("service worker/cache bumped", server.includes("nexus-behavior-496") && app.includes("nexus-behavior-496") && sw.includes("agrinexus-pwa-v441"));
 check("package alias exists", pkg.scripts["qa:nexus-internet-resource-provider-pathway"] === "node scripts/nexus-internet-resource-provider-pathway-qa.js");
 check("qa-suite safe wiring exists", qaSuite.includes("scripts/nexus-internet-resource-provider-pathway-qa.js"));
 
diff --git a/scripts/nexus-layered-internet-intelligence-qa.js b/scripts/nexus-layered-internet-intelligence-qa.js
index 492f216..1be93a5 100644
--- a/scripts/nexus-layered-internet-intelligence-qa.js
+++ b/scripts/nexus-layered-internet-intelligence-qa.js
@@ -161,15 +161,15 @@ includes(app, "No citations are shown because live retrieval is not configured",
 ].forEach(token => includes(css, token, `layered css ${token}`));
 
 [
-  "nexus-behavior-495",
-  "agrinexus-pwa-v440"
+  "nexus-behavior-496",
+  "agrinexus-pwa-v441"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
   includes(sw, token, `service worker build ${token}`);
 });
-includes(index, "/styles.css?v=nexus-behavior-495", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-495", "app cache bust");
+includes(index, "/styles.css?v=nexus-behavior-496", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-496", "app cache bust");
 
 assert.strictEqual(
   packageJson.scripts["qa:nexus-layered-internet-intelligence"],
diff --git a/scripts/nexus-live-knowledge-retrieval-qa.js b/scripts/nexus-live-knowledge-retrieval-qa.js
index fb2c870..c77755c 100644
--- a/scripts/nexus-live-knowledge-retrieval-qa.js
+++ b/scripts/nexus-live-knowledge-retrieval-qa.js
@@ -207,15 +207,15 @@ const setupDoc = read("docs/NEXUS_LIVE_KNOWLEDGE_PROVIDER_CONFIGURATION.md");
 ].forEach(token => excludes(setupDoc, token, `setup documentation secret pattern ${token}`));
 
 [
-  "nexus-behavior-495",
-  "agrinexus-pwa-v440"
+  "nexus-behavior-496",
+  "agrinexus-pwa-v441"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
   includes(sw, token, `service worker build ${token}`);
 });
-includes(index, "/styles.css?v=nexus-behavior-495", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-495", "app cache bust");
+includes(index, "/styles.css?v=nexus-behavior-496", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-496", "app cache bust");
 
 assert.strictEqual(
   packageJson.scripts["qa:nexus-live-knowledge-retrieval"],
diff --git a/scripts/nexus-panel-tab-click-repair-qa.js b/scripts/nexus-panel-tab-click-repair-qa.js
index 972b8eb..24c7663 100644
--- a/scripts/nexus-panel-tab-click-repair-qa.js
+++ b/scripts/nexus-panel-tab-click-repair-qa.js
@@ -137,16 +137,16 @@ formButtonMatches.forEach(buttonHtml => {
 });
 
 [
-  ["nexus-behavior-495", "app build version", "server build version"],
-  ["agrinexus-pwa-v440", "app cache version", "server cache version"]
+  ["nexus-behavior-496", "app build version", "server build version"],
+  ["agrinexus-pwa-v441", "app cache version", "server cache version"]
 ].forEach(([version, appLabel, serverLabel]) => {
   includes(app, version, `${appLabel} ${version}`);
   includes(server, version, `${serverLabel} ${version}`);
 });
-includes(sw, 'CACHE_NAME = "agrinexus-pwa-v440"', "service worker cache bump");
-includes(sw, 'BUILD_VERSION = "nexus-behavior-495"', "service worker build bump");
-includes(index, "/styles.css?v=nexus-behavior-495", "stylesheet cache bust");
-includes(index, "/app.js?v=nexus-behavior-495", "app cache bust");
+includes(sw, 'CACHE_NAME = "agrinexus-pwa-v441"', "service worker cache bump");
+includes(sw, 'BUILD_VERSION = "nexus-behavior-496"', "service worker build bump");
+includes(index, "/styles.css?v=nexus-behavior-496", "stylesheet cache bust");
+includes(index, "/app.js?v=nexus-behavior-496", "app cache bust");
 
 [
   "live emergency response enabled",
diff --git a/scripts/nexus-platform-predictive-intelligence-qa.js b/scripts/nexus-platform-predictive-intelligence-qa.js
index 8a3e818..c8b9b6d 100644
--- a/scripts/nexus-platform-predictive-intelligence-qa.js
+++ b/scripts/nexus-platform-predictive-intelligence-qa.js
@@ -204,8 +204,8 @@ domains.forEach(domain => {
   "WhatsApp sent successfully"
 ].forEach(token => excludes(`${app}\n${server}`, token, `unsafe predictive execution claim ${token}`));
 
-includes(serviceWorker, "agrinexus-pwa-v440", "service worker cache bump");
-includes(serviceWorker, "nexus-behavior-495", "service worker build version bump");
+includes(serviceWorker, "agrinexus-pwa-v441", "service worker cache bump");
+includes(serviceWorker, "nexus-behavior-496", "service worker build version bump");
 assert.strictEqual(packageJson.scripts["qa:nexus-platform-predictive-intelligence"], "node scripts/nexus-platform-predictive-intelligence-qa.js", "platform package alias should run platform QA");
 includes(qaSuite, "scripts/nexus-platform-predictive-intelligence-qa.js", "qa-suite should include platform predictive intelligence QA");
 
diff --git a/scripts/nexus-production-audio-pipeline-qa.js b/scripts/nexus-production-audio-pipeline-qa.js
index 2617eec..9df8a50 100644
--- a/scripts/nexus-production-audio-pipeline-qa.js
+++ b/scripts/nexus-production-audio-pipeline-qa.js
@@ -74,10 +74,10 @@ assert(app.includes("I will not claim I can hear audio until recognition starts.
 assert(app.includes("Microphone permission may be available, but recognition is not actively listening now."), "permission-granted recognition failure must be truthful");
 assert(app.includes("Genesis keeps the home screen audio-first"), "voice fallback copy must keep Genesis home audio-first");
 
-assert(app.includes("nexus-behavior-495"), "app build identifier must be bumped");
-assert(server.includes("nexus-behavior-495"), "server build identifier must be bumped");
-assert(app.includes("agrinexus-pwa-v440"), "app PWA cache identifier must be bumped");
-assert(server.includes("agrinexus-pwa-v440"), "server PWA cache identifier must be bumped");
+assert(app.includes("nexus-behavior-496"), "app build identifier must be bumped");
+assert(server.includes("nexus-behavior-496"), "server build identifier must be bumped");
+assert(app.includes("agrinexus-pwa-v441"), "app PWA cache identifier must be bumped");
+assert(server.includes("agrinexus-pwa-v441"), "server PWA cache identifier must be bumped");
 const autoStartBlock = app.slice(app.indexOf("async function maybeStartGenesisRecognitionAfterGrantedPermission"), app.indexOf("function nexusVoiceAudioDebugEnabled"));
 assert(autoStartBlock.includes("nexusMicrophonePermissionCanAttemptStart(permission)"), "prompt/granted/browser-managed permission states must be eligible for getUserMedia attempt");
 assert(!autoStartBlock.includes("granted-or-browser-managed"), "auto-start must not compare against legacy display labels");
diff --git a/scripts/nexus-production-platform-rails-qa.js b/scripts/nexus-production-platform-rails-qa.js
index e5d12f0..477127f 100644
--- a/scripts/nexus-production-platform-rails-qa.js
+++ b/scripts/nexus-production-platform-rails-qa.js
@@ -137,8 +137,8 @@ function excludes(source, token, label) {
 ].forEach(token => includes(css, token, `production rail CSS ${token}`));
 
 [
-  "nexus-behavior-495",
-  "agrinexus-pwa-v440"
+  "nexus-behavior-496",
+  "agrinexus-pwa-v441"
 ].forEach(token => {
   includes(app, token, `app build ${token}`);
   includes(server, token, `server build ${token}`);
diff --git a/scripts/nexus-realtime-microphone-visibility-qa.js b/scripts/nexus-realtime-microphone-visibility-qa.js
index 37c35b6..a1feff1 100644
--- a/scripts/nexus-realtime-microphone-visibility-qa.js
+++ b/scripts/nexus-realtime-microphone-visibility-qa.js
@@ -39,18 +39,18 @@ const realtimeStartup = between(app, "async function startOpenAiAgentsRealtimeVo
 const checks = [
   [
     "Build and cache advanced",
-    app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"') &&
-      app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"') &&
-      server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495"') &&
-      sw.includes('const CACHE_NAME = "agrinexus-pwa-v440"') &&
-      index.includes("/app.js?v=nexus-behavior-495")
+    app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"') &&
+      app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"') &&
+      server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496"') &&
+      sw.includes('const CACHE_NAME = "agrinexus-pwa-v441"') &&
+      index.includes("/app.js?v=nexus-behavior-496")
   ],
   [
     "Static HTML microphone exists before app JavaScript",
     index.includes('id="nexusPermanentMicrophoneDock"') &&
       index.includes('id="nexusPermanentMicrophoneBtn"') &&
       index.includes("Enable microphone") &&
-      index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf('/app.js?v=nexus-behavior-495') &&
+      index.indexOf('id="nexusPermanentMicrophoneBtn"') < index.indexOf('/app.js?v=nexus-behavior-496') &&
       !/id="nexusPermanentMicrophoneBtn"[^>]*(hidden|disabled)/i.test(index)
   ],
   [
diff --git a/scripts/nexus-service-worker-cache-safety-qa.js b/scripts/nexus-service-worker-cache-safety-qa.js
index 42297a4..5259337 100644
--- a/scripts/nexus-service-worker-cache-safety-qa.js
+++ b/scripts/nexus-service-worker-cache-safety-qa.js
@@ -13,8 +13,8 @@ function includesAll(source, tokens, label) {
 }
 
 includesAll(sw, [
-  'const CACHE_NAME = "agrinexus-pwa-v440"',
-  'const BUILD_VERSION = "nexus-behavior-495"',
+  'const CACHE_NAME = "agrinexus-pwa-v441"',
+  'const BUILD_VERSION = "nexus-behavior-496"',
   "function isCacheableApplicationRequest",
   '["http:", "https:"].includes(url.protocol)',
   "url.origin !== self.location.origin",
@@ -49,8 +49,8 @@ assert(sw.includes(".catch(error =>"), "install cache failures must be handled")
 
 includesAll(app, [
   'const NEXUS_GENESIS_VOICE_RUNTIME_VERSION = "nexus-genesis-voice-runtime-v456"',
-  'const AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"',
-  'const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"',
+  'const AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"',
+  'const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"',
   "console.info(`[Nexus Genesis voice] ${stage}",
   "controller-initialized",
   "automatic-start-entered",
diff --git a/scripts/nexus-standard-user-home-screen-redesign-qa.js b/scripts/nexus-standard-user-home-screen-redesign-qa.js
index 45648a2..4bcacb5 100644
--- a/scripts/nexus-standard-user-home-screen-redesign-qa.js
+++ b/scripts/nexus-standard-user-home-screen-redesign-qa.js
@@ -319,14 +319,14 @@ function excludes(source, token, label) {
 ].forEach(token => excludes(app, token, "Standard User home screen"));
 
 [
-  [app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"', "app build version"],
-  [app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"', "app cache version"],
-  [server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495"', "server build version"],
-  [server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"', "server cache version"],
-  [sw, 'CACHE_NAME = "agrinexus-pwa-v440"', "service worker cache"],
-  [sw, 'BUILD_VERSION = "nexus-behavior-495"', "service worker build"],
-  [index, "/styles.css?v=nexus-behavior-495", "stylesheet cache bust"],
-  [index, "/app.js?v=nexus-behavior-495", "app script cache bust"]
+  [app, 'AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"', "app build version"],
+  [app, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"', "app cache version"],
+  [server, 'AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496"', "server build version"],
+  [server, 'AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"', "server cache version"],
+  [sw, 'CACHE_NAME = "agrinexus-pwa-v441"', "service worker cache"],
+  [sw, 'BUILD_VERSION = "nexus-behavior-496"', "service worker build"],
+  [index, "/styles.css?v=nexus-behavior-496", "stylesheet cache bust"],
+  [index, "/app.js?v=nexus-behavior-496", "app script cache bust"]
 ].forEach(([source, token, label]) => includes(source, token, label));
 
 assert.strictEqual(
diff --git a/scripts/nexus-system-wide-integrity-hardening-qa.js b/scripts/nexus-system-wide-integrity-hardening-qa.js
index 6e23c04..09ce710 100644
--- a/scripts/nexus-system-wide-integrity-hardening-qa.js
+++ b/scripts/nexus-system-wide-integrity-hardening-qa.js
@@ -277,19 +277,19 @@ function assertEnvelope(envelope, label, correlationId) {
   ].forEach(token => assert(serverSource.includes(token), `server missing ${token}`));
   FAILURE_CATEGORIES.forEach(category => assert(serverSource.includes(category), `failure taxonomy missing ${category}`));
   [
-    "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-495\"",
-    "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v440\"",
+    "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-496\"",
+    "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v441\"",
     "const NEXUS_GENESIS_VOICE_RUNTIME_VERSION = \"nexus-genesis-voice-runtime-v456\"",
     "payload?.nexusResponse || payload?.genesisResponse"
   ].forEach(token => assert(appSource.includes(token), `app missing ${token}`));
   [
-    "const CACHE_NAME = \"agrinexus-pwa-v440\"",
-    "const BUILD_VERSION = \"nexus-behavior-495\""
+    "const CACHE_NAME = \"agrinexus-pwa-v441\"",
+    "const BUILD_VERSION = \"nexus-behavior-496\""
   ].forEach(token => assert(swSource.includes(token), `service worker missing ${token}`));
   [
-    "/app.js?v=nexus-behavior-495",
-    "/styles.css?v=nexus-behavior-495",
-    "/manifest.webmanifest?v=nexus-behavior-495"
+    "/app.js?v=nexus-behavior-496",
+    "/styles.css?v=nexus-behavior-496",
+    "/manifest.webmanifest?v=nexus-behavior-496"
   ].forEach(token => assert(indexSource.includes(token), `index missing ${token}`));
   assert(appSource.includes("startVoiceListening({ source: \"genesis-home-permission-granted-auto-start\" })"), "Genesis mic auto-start must remain intact");
   assert(appSource.includes("startRealtimeVoiceSession"), "OpenAI Realtime start must remain intact");
diff --git a/scripts/nexus-true-experience-qa-common.js b/scripts/nexus-true-experience-qa-common.js
index 40a3aae..e2ff430 100644
--- a/scripts/nexus-true-experience-qa-common.js
+++ b/scripts/nexus-true-experience-qa-common.js
@@ -143,11 +143,11 @@ function assertContextualMission() {
 }
 
 function assertCacheResponsive() {
-  assert(app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-495"'), "app build version bumped");
-  assert(app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"'), "app cache version bumped");
-  assert(server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495"'), "server build version bumped");
-  assert(server.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440"'), "server cache version bumped");
-  assert(sw.includes('const CACHE_NAME = "agrinexus-pwa-v440"'), "service worker cache version bumped");
+  assert(app.includes('const AGRINEXUS_BUILD_VERSION = "nexus-behavior-496"'), "app build version bumped");
+  assert(app.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"'), "app cache version bumped");
+  assert(server.includes('const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496"'), "server build version bumped");
+  assert(server.includes('const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441"'), "server cache version bumped");
+  assert(sw.includes('const CACHE_NAME = "agrinexus-pwa-v441"'), "service worker cache version bumped");
   assert(styles.includes("@media (max-width: 520px)"), "small mobile viewport styles exist");
   assert(styles.includes("@media (max-height: 640px) and (min-width: 700px)"), "short desktop viewport styles exist");
   assert(styles.includes("overflow-x: hidden"), "horizontal overflow is guarded");
diff --git a/scripts/nexus-world-demonstration-release-qa.js b/scripts/nexus-world-demonstration-release-qa.js
index 7ba6dcd..365fc0d 100644
--- a/scripts/nexus-world-demonstration-release-qa.js
+++ b/scripts/nexus-world-demonstration-release-qa.js
@@ -113,21 +113,21 @@ const submitRouting = between(app, "function routeNexusCommandCenterCommunicatio
 assert(submitRouting.indexOf("handleNexusVoiceTroubleshootingCommand(command, { source })") < submitRouting.indexOf("advanceNexusOsMissionForCommand(command, { source });"), "voice status commands must run before mission planning");
 
 includesAll(index, [
-  "/manifest.webmanifest?v=nexus-behavior-495",
-  "/styles.css?v=nexus-behavior-495",
-  "/app.js?v=nexus-behavior-495"
+  "/manifest.webmanifest?v=nexus-behavior-496",
+  "/styles.css?v=nexus-behavior-496",
+  "/app.js?v=nexus-behavior-496"
 ], "index cache bust");
 includesAll(app, [
-  "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-495\";",
-  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v440\";"
+  "const AGRINEXUS_BUILD_VERSION = \"nexus-behavior-496\";",
+  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v441\";"
 ], "app cache bust");
 includesAll(sw, [
-  "const CACHE_NAME = \"agrinexus-pwa-v440\";",
-  "const BUILD_VERSION = \"nexus-behavior-495\";"
+  "const CACHE_NAME = \"agrinexus-pwa-v441\";",
+  "const BUILD_VERSION = \"nexus-behavior-496\";"
 ], "service worker cache bust");
 includesAll(server, [
-  "const AGRINEXUS_WEB_BUILD_VERSION = \"nexus-behavior-495\";",
-  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v440\";"
+  "const AGRINEXUS_WEB_BUILD_VERSION = \"nexus-behavior-496\";",
+  "const AGRINEXUS_PWA_CACHE_VERSION = \"agrinexus-pwa-v441\";"
 ], "server cache bust");
 
 assert.strictEqual(
diff --git a/scripts/qa-suite.js b/scripts/qa-suite.js
index 637ec59..e8d2118 100644
--- a/scripts/qa-suite.js
+++ b/scripts/qa-suite.js
@@ -770,6 +770,7 @@ const suites = {
     "scripts/nexus-rp6-shipment-tracking-real-provider-activation-qa.js",
     "scripts/nexus-rp7-music-media-real-provider-activation-qa.js",
     "scripts/nexus-youtube-data-api-live-provider-qa.js",
+    "scripts/nexus-google-cloud-translation-provider-qa.js",
     "scripts/nexus-rp8-unified-provider-live-mock-skip-matrix-qa.js",
     "scripts/nexus-rp9-controlled-assistant-preview-live-provider-sweep-qa.js",
     "scripts/nexus-rp10-standard-user-live-preview-readiness-validation-qa.js",
@@ -1618,6 +1619,7 @@ suites["all-safe"] = unique([
   "scripts/nexus-rp6-shipment-tracking-real-provider-activation-qa.js",
   "scripts/nexus-rp7-music-media-real-provider-activation-qa.js",
   "scripts/nexus-youtube-data-api-live-provider-qa.js",
+  "scripts/nexus-google-cloud-translation-provider-qa.js",
   "scripts/nexus-rp8-unified-provider-live-mock-skip-matrix-qa.js",
   "scripts/nexus-rp9-controlled-assistant-preview-live-provider-sweep-qa.js",
   "scripts/nexus-rp10-standard-user-live-preview-readiness-validation-qa.js",
diff --git a/server.js b/server.js
index 0699481..bda0a0c 100644
--- a/server.js
+++ b/server.js
@@ -35,6 +35,7 @@ const nexusOsHealthWorkforceSafetyPack = require("./public/nexus-os-health-workf
 const nexusOsHealthNexusReferenceProfile = require("./public/nexus-os-healthnexus-reference-profile.js");
 const nexusOsControlPlane = require("./server/nexusOsControlPlane.js");
 const nexusWeatherSourceProvider = require("./server/nexus-weather-source-provider.js");
+const googleCloudTranslationProvider = require("./server/google-cloud-translation-provider.js");
 const {
   isUsableEnvValue,
   loadLocalEnvFiles
@@ -53,8 +54,8 @@ const AI_MODEL = process.env.OPENAI_MODEL || "gpt-5.4-mini";
 const AI_REASONING_MODEL = process.env.OPENAI_REASONING_MODEL || process.env.OPENAI_AGENT_MODEL || AI_MODEL;
 const AI_TRANSLATION_MODEL = process.env.OPENAI_TRANSLATION_MODEL || process.env.OPENAI_AGENT_MODEL || AI_MODEL;
 const AGRINEXUS_RELEASE = "2026-06-16-operational-readiness";
-const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-495";
-const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v440";
+const AGRINEXUS_WEB_BUILD_VERSION = "nexus-behavior-496";
+const AGRINEXUS_PWA_CACHE_VERSION = "agrinexus-pwa-v441";
 const NEXUS_GENESIS_REALTIME_RUNTIME_VERSION = "nexus-genesis-openai-agents-realtime-v3";
 const NEXUS_GENESIS_VOICE_RUNTIME_VALUES = new Set(["realtime", "disabled"]);
 const NEXUS_GENESIS_REALTIME_FALLBACK_VALUES = new Set(["blocked"]);
@@ -929,7 +930,7 @@ const PROVIDER_CONFIG = {
   "voice-stt": { modeEnv: "VOICE_STT_PROVIDER", credentialEnvs: ["VOICE_STT_WEBHOOK_URL", "VOICE_PROVIDER_API_KEY"] },
   "voice-tts": { modeEnv: "VOICE_TTS_PROVIDER", credentialEnvs: ["VOICE_TTS_WEBHOOK_URL", "VOICE_PROVIDER_API_KEY"] },
   "phone-voice": { modeEnv: "PHONE_PROVIDER", credentialEnvs: ["TWILIO_ACCOUNT_SID", "TWILIO_AUTH_TOKEN", "TWILIO_PHONE_NUMBER"] },
-  "translation": { modeEnv: "TRANSLATION_PROVIDER", credentialEnvs: ["TRANSLATION_WEBHOOK_URL", "TRANSLATION_PROVIDER_API_KEY"] },
+  "translation": { modeEnv: "TRANSLATION_PROVIDER", credentialEnvs: ["GOOGLE_TRANSLATION_CREDENTIALS_JSON", "TRANSLATION_WEBHOOK_URL", "TRANSLATION_PROVIDER_API_KEY"] },
   "auth-users": { modeEnv: "AUTH_PROVIDER", credentialEnvs: ["AUTH_WEBHOOK_URL", "AUTH_PROVIDER_API_KEY"] },
   "auth-password-reset": { modeEnv: "PASSWORD_RESET_PROVIDER", credentialEnvs: ["PASSWORD_RESET_WEBHOOK_URL", "AUTH_PROVIDER_API_KEY"] },
   "email-delivery": { modeEnv: "EMAIL_PROVIDER", credentialEnvs: ["EMAIL_WEBHOOK_URL", "COMMUNICATION_PROVIDER_API_KEY"] },
@@ -6482,6 +6483,7 @@ function runtimeProviders(db) {
       const hasCredential = Boolean(runtime.webhookUrl && runtime.apiKey);
       const hasBridge = Boolean(providerEngineWebhookUrl(provider.id) && runtime.apiKey);
       const hasOpenAiVoice = isOpenAiVoice && Boolean(process.env.OPENAI_API_KEY);
+      const hasGoogleTranslation = provider.id === "translation" && runtime.googleTranslationConfigured === true;
       const hasTwilioVoice = provider.id === "phone-voice"
         && mode === "twilio"
         && Boolean(process.env.TWILIO_ACCOUNT_SID)
@@ -6494,8 +6496,10 @@ function runtimeProviders(db) {
       return {
         ...provider,
         mode,
-        status: hasOpenAiVoice || hasTwilioVoice || (hasTwilioMessaging && twilioMessagingRecipient) ? "connected" : hasTwilioMessaging ? "needs-recipient" : isBrowser ? (REQUIRE_LIVE_SERVICES ? "needs-live-provider" : "connected") : isSandbox ? (REQUIRE_LIVE_SERVICES ? "needs-live-provider" : "connected") : (hasCredential ? "connected" : "needs-credentials"),
-        detail: hasOpenAiVoice
+        status: hasGoogleTranslation || hasOpenAiVoice || hasTwilioVoice || (hasTwilioMessaging && twilioMessagingRecipient) ? "connected" : hasTwilioMessaging ? "needs-recipient" : isBrowser ? (REQUIRE_LIVE_SERVICES ? "needs-live-provider" : "connected") : isSandbox ? (REQUIRE_LIVE_SERVICES ? "needs-live-provider" : "connected") : (hasCredential ? "connected" : "needs-credentials"),
+        detail: hasGoogleTranslation
+          ? "Google Cloud Translation v3 is configured with server-only service-account credentials."
+          : hasOpenAiVoice
           ? `${mode} provider configured through OPENAI_API_KEY.`
           : hasTwilioVoice
           ? "Twilio phone assistant is configured with account credentials, phone number, and PUBLIC_BASE_URL."
@@ -6537,7 +6541,7 @@ function integrationStatus(db) {
       database: ["DATABASE_URL", "AGRINEXUS_STATE_STORE=postgres"],
       security: ["SESSION_SECRET", "PASSWORD_PEPPER"],
       ai: ["OPENAI_API_KEY or AI_PROVIDER=webhook + AI_WEBHOOK_URL + AI_PROVIDER_API_KEY"],
-      translation: ["TRANSLATION_PROVIDER", "TRANSLATION_WEBHOOK_URL", "TRANSLATION_PROVIDER_API_KEY"],
+      translation: ["GOOGLE_TRANSLATION_CREDENTIALS_JSON or TRANSLATION_PROVIDER + TRANSLATION_WEBHOOK_URL + TRANSLATION_PROVIDER_API_KEY"],
       learning: ["LEARNING_COURSE_PROVIDER", "LEARNING_CERTIFICATE_PROVIDER", "LEARNING_*_WEBHOOK_URL", "LEARNING_PROVIDER_API_KEY"],
       workforce: ["WORKFORCE_JOB_PROVIDER", "WORKFORCE_CALENDAR_PROVIDER", "WORKFORCE_NOTIFICATION_PROVIDER", "WORKFORCE_HRIS_PROVIDER", "WORKFORCE_SHIFT_PROVIDER", "WORKFORCE_*_WEBHOOK_URL", "WORKFORCE_PROVIDER_API_KEY"],
       healthcare: ["HEALTH_TELEHEALTH_PROVIDER", "HEALTH_EHR_PROVIDER", "HEALTH_NOTIFICATION_PROVIDER", "HEALTH_*_WEBHOOK_URL", "HEALTH_PROVIDER_API_KEY"],
@@ -8997,10 +9001,12 @@ function providerRuntime(providerId) {
   const twilioMessagingProvider = ["sms-delivery", "whatsapp-delivery"].includes(providerId) && hasTwilioMessagingCore();
   const explicitWebhook = webhookKey ? process.env[webhookKey] || "" : "";
   const bridgeWebhook = providerEngineWebhookUrl(providerId);
+  const googleTranslationConfigured = providerId === "translation" && googleCloudTranslationProvider.isConfigured(process.env);
   return {
-    mode: process.env[config.modeEnv] || (openAiVoiceProvider ? "openai" : twilioMessagingProvider ? "twilio" : "sandbox"),
+    mode: process.env[config.modeEnv] || (googleTranslationConfigured ? "google-cloud-translation" : openAiVoiceProvider ? "openai" : twilioMessagingProvider ? "twilio" : "sandbox"),
     webhookUrl: explicitWebhook || bridgeWebhook,
-    apiKey: apiKey ? process.env[apiKey] || "" : ""
+    apiKey: apiKey ? process.env[apiKey] || "" : "",
+    googleTranslationConfigured
   };
 }
 
@@ -16770,6 +16776,7 @@ function openAiRealtimeInstructions(user, language = "en") {
     "Ordinary conversation remains conversation. Never open, create, continue, or display a workflow merely because the user speaks.",
     "Answer greetings, presence checks, emotional support, casual questions, capability questions, and contextual follow-ups directly without a function tool.",
     "Use tools only when a real Nexus capability is needed, such as weather, maps, agriculture, health preparation, workforce, learning, marketplace, logistics, communications, provider readiness, receipts, or workflow preparation.",
+    "When the user explicitly asks Nexus to translate text or change language and say a phrase, you must call nexus_translation with the complete request and the requested language code. Use the provider-backed translation returned by Nexus.",
     "When the user explicitly asks to open, show, display, or use Maps or requests a route or directions, you must call nexus_maps_route with the user's complete request. Never answer that you cannot open a Maps app. Nexus opens its own browser Maps workspace; an unavailable external route provider does not prevent that workspace from opening.",
     "Never claim an action completed without verified evidence from the Nexus tool result.",
     "High-risk actions require Nexus confirmation gates. Do not send messages, call, schedule, pay, dispatch, refill, diagnose, prescribe, contact providers, share location, or execute marketplace actions by inference.",
@@ -17060,7 +17067,8 @@ async function dispatchNexusRealtimeTool(db, user, body = {}) {
     executionAttempted: Boolean(envelope.execution?.attempted),
     executionVerified: Boolean(envelope.execution?.verified),
     missingInformation: envelope.missingInformation || [],
-    blockedReason: envelope.blockedReason || null
+    blockedReason: envelope.blockedReason || null,
+    translationProvider: result?.metadata?.translation?.provider || null
   };
 }
 
@@ -17132,6 +17140,7 @@ function nexusOpenAiNativeToolSchemas() {
   });
   return [
     tool("nexus_general_conversation", "Use Nexus shared conversation, contextual follow-up, clarification, correction, language, and capability explanation without forcing a workflow.", "conversation"),
+    tool("nexus_translation", "Translate user-requested text or change language through the configured Nexus translation provider. Pass the requested target language in the language argument.", "read-only-provider"),
     tool("nexus_live_knowledge", "Run Nexus Live Knowledge retrieval for current, source-backed, citation-sensitive, or research questions. Returns truthful credential-blocked/provider-error states when not configured.", "read-only-source"),
     tool("nexus_weather", "Run Nexus weather and forecast support using configured read-only weather providers or truthful missing-provider/missing-location states.", "read-only-source"),
     tool("nexus_maps_route", "Run Nexus maps, route, logistics, field visit, or typed-location support without browser geolocation, dispatch, or sharing precise location.", "read-only-or-preparation"),
@@ -17247,6 +17256,7 @@ function nexusOpenAiNativeStatus(env = process.env) {
 function nexusOpenAiNativeToolChoiceHint(command = "") {
   const lower = String(command || "").toLowerCase();
   if (/\b(weather|forecast|temperature|rain|heat index)\b/.test(lower)) return "nexus_weather";
+  if (/\b(translate|translation|change language|speak in|say .* in (?:swahili|french|spanish|arabic|portuguese))\b/.test(lower)) return "nexus_translation";
   if (/\b(current|latest|today|now|recent|source|sources|cite|citation|research|look up|search)\b/.test(lower)) return "nexus_live_knowledge";
   if (/\b(deep research|research brief|multi-source|compare sources|evidence review|literature|institutional evidence)\b/.test(lower)) return "nexus_deep_research";
   if (/\b(file|document|pdf|word|spreadsheet|excel|csv|presentation|powerpoint|upload|attachment)\b/.test(lower)) return "nexus_file_document_analysis";
@@ -18196,7 +18206,20 @@ async function translateDynamicContent(db, user, { text, targetLanguage, sourceL
     ? `[${String(targetLanguage).toUpperCase()}] ${String(text || "")}`
     : localTranslateText(text, targetLanguage);
   let provider = "local-dictionary";
-  if (runtime.mode !== "local-dictionary" && runtime.mode !== "sandbox" && runtime.webhookUrl && runtime.apiKey) {
+  if (runtime.googleTranslationConfigured && targetLanguage && targetLanguage !== sourceLanguage) {
+    try {
+      const googleResult = await googleCloudTranslationProvider.translateText({
+        text,
+        sourceLanguage,
+        targetLanguage
+      });
+      translatedText = googleResult.translatedText;
+      provider = googleResult.provider;
+    } catch {
+      provider = "local-after-google-translation-error";
+    }
+  }
+  if (provider !== "google-cloud-translation" && runtime.mode !== "local-dictionary" && runtime.mode !== "sandbox" && runtime.webhookUrl && runtime.apiKey) {
     try {
       const response = await fetchWithTimeout(runtime.webhookUrl, {
         method: "POST",
@@ -18215,7 +18238,7 @@ async function translateDynamicContent(db, user, { text, targetLanguage, sourceL
       provider = "local-after-translation-error";
     }
   }
-  if ((provider === "local-dictionary" || provider === "local-after-translation-error") && process.env.OPENAI_API_KEY && targetLanguage && targetLanguage !== sourceLanguage) {
+  if ((provider === "local-dictionary" || provider === "local-after-translation-error" || provider === "local-after-google-translation-error") && process.env.OPENAI_API_KEY && targetLanguage && targetLanguage !== sourceLanguage) {
     try {
       const response = await fetchWithTimeout("https://api.openai.com/v1/responses", {
         method: "POST",
diff --git a/server/google-cloud-translation-provider.js b/server/google-cloud-translation-provider.js
new file mode 100644
index 0000000..00ec2d2
--- /dev/null
+++ b/server/google-cloud-translation-provider.js
@@ -0,0 +1,148 @@
+"use strict";
+
+const crypto = require("crypto");
+
+const GOOGLE_OAUTH_TOKEN_URL = "https://oauth2.googleapis.com/token";
+const GOOGLE_TRANSLATION_SCOPE = "https://www.googleapis.com/auth/cloud-translation";
+const tokenCache = new Map();
+
+function base64Url(value) {
+  return Buffer.from(value)
+    .toString("base64")
+    .replace(/\+/g, "-")
+    .replace(/\//g, "_")
+    .replace(/=+$/g, "");
+}
+
+function parseCredentials(env = process.env) {
+  const raw = String(env.GOOGLE_TRANSLATION_CREDENTIALS_JSON || "").trim();
+  if (!raw) return null;
+  let credentials;
+  try {
+    credentials = JSON.parse(raw);
+  } catch {
+    const error = new Error("Google Translation credentials JSON is invalid.");
+    error.code = "invalid-credentials-json";
+    throw error;
+  }
+  if (
+    credentials.type !== "service_account"
+    || !credentials.project_id
+    || !credentials.client_email
+    || !credentials.private_key
+  ) {
+    const error = new Error("Google Translation service-account credentials are incomplete.");
+    error.code = "incomplete-service-account";
+    throw error;
+  }
+  return credentials;
+}
+
+function isConfigured(env = process.env) {
+  try {
+    return Boolean(parseCredentials(env));
+  } catch {
+    return false;
+  }
+}
+
+function createServiceAccountAssertion(credentials, nowSeconds = Math.floor(Date.now() / 1000)) {
+  const tokenUri = credentials.token_uri || GOOGLE_OAUTH_TOKEN_URL;
+  const header = base64Url(JSON.stringify({ alg: "RS256", typ: "JWT" }));
+  const claims = base64Url(JSON.stringify({
+    iss: credentials.client_email,
+    scope: GOOGLE_TRANSLATION_SCOPE,
+    aud: tokenUri,
+    iat: nowSeconds,
+    exp: nowSeconds + 3600
+  }));
+  const unsigned = `${header}.${claims}`;
+  const signature = crypto.sign("RSA-SHA256", Buffer.from(unsigned), credentials.private_key);
+  return `${unsigned}.${base64Url(signature)}`;
+}
+
+async function accessToken(credentials, fetchImpl = global.fetch) {
+  const cacheKey = `${credentials.project_id}:${credentials.client_email}`;
+  const cached = tokenCache.get(cacheKey);
+  if (cached && cached.expiresAt > Date.now() + 60_000) return cached.value;
+
+  const tokenUri = credentials.token_uri || GOOGLE_OAUTH_TOKEN_URL;
+  const assertion = createServiceAccountAssertion(credentials);
+  const response = await fetchImpl(tokenUri, {
+    method: "POST",
+    headers: { "content-type": "application/x-www-form-urlencoded" },
+    body: new URLSearchParams({
+      grant_type: "urn:ietf:params:oauth:grant-type:jwt-bearer",
+      assertion
+    }).toString()
+  });
+  const payload = await response.json().catch(() => ({}));
+  if (!response.ok || !payload.access_token) {
+    const error = new Error(`Google OAuth token request failed (${response.status || "unknown"}).`);
+    error.code = "google-oauth-failed";
+    throw error;
+  }
+  tokenCache.set(cacheKey, {
+    value: payload.access_token,
+    expiresAt: Date.now() + Math.max(60, Number(payload.expires_in || 3600)) * 1000
+  });
+  return payload.access_token;
+}
+
+async function translateText({
+  text,
+  targetLanguage,
+  sourceLanguage = "en",
+  env = process.env,
+  fetchImpl = global.fetch
+}) {
+  const credentials = parseCredentials(env);
+  if (!credentials) {
+    const error = new Error("Google Translation credentials are not configured.");
+    error.code = "google-translation-not-configured";
+    throw error;
+  }
+  const accessTokenValue = await accessToken(credentials, fetchImpl);
+  const projectId = encodeURIComponent(credentials.project_id);
+  const endpoint = `https://translation.googleapis.com/v3/projects/${projectId}/locations/global:translateText`;
+  const response = await fetchImpl(endpoint, {
+    method: "POST",
+    headers: {
+      authorization: `Bearer ${accessTokenValue}`,
+      "content-type": "application/json; charset=utf-8"
+    },
+    body: JSON.stringify({
+      contents: [String(text || "")],
+      mimeType: "text/plain",
+      sourceLanguageCode: String(sourceLanguage || "en"),
+      targetLanguageCode: String(targetLanguage || "en")
+    })
+  });
+  const payload = await response.json().catch(() => ({}));
+  const translatedText = String(payload?.translations?.[0]?.translatedText || "").trim();
+  if (!response.ok || !translatedText) {
+    const error = new Error(`Google Cloud Translation request failed (${response.status || "unknown"}).`);
+    error.code = "google-translation-failed";
+    throw error;
+  }
+  return {
+    translatedText,
+    provider: "google-cloud-translation",
+    sourceLanguage: String(sourceLanguage || "en"),
+    targetLanguage: String(targetLanguage || "en")
+  };
+}
+
+function clearTokenCache() {
+  tokenCache.clear();
+}
+
+module.exports = Object.freeze({
+  GOOGLE_OAUTH_TOKEN_URL,
+  GOOGLE_TRANSLATION_SCOPE,
+  parseCredentials,
+  isConfigured,
+  createServiceAccountAssertion,
+  translateText,
+  clearTokenCache
+});
-- 
2.51.1

