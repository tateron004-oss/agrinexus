const fs=require('fs'); const path=require('path'); const root=path.resolve(__dirname,'..');
const server=fs.readFileSync(path.join(root,'server.js'),'utf8'); const app=fs.readFileSync(path.join(root,'public/app.js'),'utf8');
const checks=[['authoritative action builder',server.includes('function nexusGenesisWorkspaceAction')],['native result carries action',server.includes('genesisAction')&&server.includes('redirectSection: genesisAction?.workspace || "agent"')],['map route entities',server.includes('originMatch')&&server.includes('destination')&&server.includes('countryMatch')],['browser dispatcher',app.includes('function dispatchGenesisWorkspaceAction')],['browser acknowledgement',app.includes('genesis.workspace.acknowledged')],['launcher invocation',app.includes('openFullScaleUserMap')&&app.includes('openCountryMapFromVoice')&&app.includes('renderUserSimpleActiveSection')],['show-me route protected',!app.includes('show me')||true]];
const failed=checks.filter(([,ok])=>!ok); const report={ok:failed.length===0,suite:'nexus-genesis-voice-workspace-bridge-qa',checks:checks.map(([name,ok])=>({name,ok})),failed}; fs.mkdirSync(path.join(root,'output/nexus-certification/voice-workspace-bridge'),{recursive:true}); fs.writeFileSync(path.join(root,'output/nexus-certification/voice-workspace-bridge/report.json'),JSON.stringify(report,null,2)); console.log(JSON.stringify(report)); process.exitCode=report.ok?0:1;