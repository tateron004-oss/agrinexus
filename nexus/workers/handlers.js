// Real handlers are registered here only after their provider/storage adapter is connected.
// An empty registry is intentional: disconnected capabilities must never simulate success.
module.exports = Object.freeze({});
